# GitHub 整理与版本说明

2026-09-22 状态：本页保留 2026-09-11 的上传历史。当前唯一开发主线为 `codex/vnext-maf`，旧本地分支已归档；新工作区入口与恢复方式见[开发线与历史归档](development-line.md)。本次没有推送或修改 GitHub 默认分支，以下“克隆默认分支即可得到最新完整项目”不再作为当前开发指引。

日期：2026-09-11。本次仅整理文档与上传，不修改业务代码、不启动服务或模型调用。

## 来源与分支

- `codex/github-upload`：GitHub 默认入口，基于最新 W1 记录提交 `40472c6`，补充 README 与克隆使用说明。
- `codex/phase-2-web-assessment`：W1 阶段来源。业务被测 SHA 为 `f12a46fd904299960609c9856e1b988494cace49`，验收记录提交为 `40472c6`。
- `codex/phase-1c-core-loop@b79efa6`：此前核心闭环交付。
- `codex/phase-1b-b23@381ae3a`：本机主目录保留的旧运行版本。
- `master@28fcd44`：历史阶段基线，保持原样，不代表最新实现。

所有已有本地分支和标签随本次上传保留，不强行合并历史开发分支。克隆默认分支即可得到最新完整项目；需要复核旧阶段时切换对应分支或 SHA。原机器的工作树目录不作为仓库嵌套副本上传。

## 未提交文件核对

主目录 AGENTS.md 与 W1 版本一致；主目录 project-context.md 是指向其他工作树的本机导航。旧 phase-1c-prep 中的 Harness、评估模型和执行交接修改已包含在 W1 中；其 W1 草案由当前 implemented/accepted 文档替代。保留原工作树的未提交状态，避免移动绑定运行环境的 HEAD。

## 上传范围

包含源码、契约及生成文件、数据库迁移、部署定义、构建和测试脚本、依赖锁文件、设计文档和阶段验收记录。继续排除 node_modules、虚拟环境、本机 CodeGraph 索引、work、artifacts、环境密钥文件、日志及运行数据。

新克隆必须按本地开发说明重新准备依赖与私有配置。历史记录中的 Artifact 路径仅是原验收证据位置，克隆不含原始证据、数据库或会话；记录的测试结论仍只适用于所列被测 SHA，不代表本次重新执行了业务验收。

## 本次最小检查

上传前核对所有工作树状态及差异，检查 Git 历史敏感信息、受跟踪文件与大文件，执行文档 diff/入口链接检查。上传后核对 GitHub 可见性、默认分支及所有分支/标签的远端 SHA。业务代码未变，不重跑后端、Kubernetes 或完整回归。

## 项目说明同步（2026-09-11）

在整理基准0460211之后，更新产品/视觉说明、主架构、Cairn/Harness/黑板设计、评估与前端边界、API 0.5导航、开发顺序和克隆启动说明。W1实施合同首页由in-progress纠正为implemented，并引用原验收；不改写被测SHA或测试结果。

核对来源为当前OpenAPI 0.5.0、迁移0008、实际受跟踪目录、锁定依赖组与构建入口，以及核心/W1验收记录。本次只做文档差异、链接、命令路径和敏感信息检查，业务代码不变，未重新执行安装、构建、Kubernetes或模型测试。当前上传工作树无CodeGraph索引，代码入口使用rg与直接读取核对；纯文档不重建索引。
