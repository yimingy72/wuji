# P10 requested shared ports for actual MAF child

2026-09-13, interface request only. P10 core owns no M1/P09/shared changes. Current M1 working copy has `PlatformWorkerHost.resolve(..., verified_principal=...)` and `entrypoint.run_assignment(..., token_verifier=...)`; the earlier M1 interface predates these SOL fixes. Dirac owns its final contract. P09 Archimedes owns credential/receiver/Scheduler fixes. Use their final ports rather than rewriting either surface here.

1. **Current execution/control authorization**: private receiver-authenticated adapter reads the actual P09 `DispatchRepository.read(tx, operation_id=...)` in receiver's observe tx, matches complete assignment/receiver/environment/pod, checks current P05 task/run/work epochs and permission before new start; query and historical stop require current receiver/controller authority, never revoked Worker bearer. Return the bounded grant in P10-core-interface. Missing adapter fails closed. No new ACL/schema requested from core. Main allocates any narrow receiver/pod registration gap to P09/SOL.
2. **Restricted bootstrap**: real `RunCredentialIssuer.retrieve(tx, credential_ref=..., identity=...)` stays behind the actual registered receiver boundary. Node bootstrap writes only per-Run credential and frozen assignment plus fixed host/Gate endpoints to private Worker-readable files. No token in argv, Outbox, log or public receipt; no controller DB/Task key in child. P10 supplies a trusted deployment bootstrap hook; main/SOL supplies the concrete restricted retrieval transport and retention. HTTP client bootstrap data must travel on authenticated local/private transport, never product wire or Actor body.
3. **Same MAF Worker subprocess**: add a narrow child launcher/remote host adapter in a newly allocated M1-owned module after Dirac release. Call actual existing `run_assignment`, `MafRuntime`, native factory; do not create another agent loop. Resolve validated ContextBundle/frozen configuration using current principal verification. Public verification key refs may be supplied; signing/decryption keys remain controller-side. Configure registered absolute Python entrypoint/argv/env under the frozen harness profile; Node never chooses command from Worker/model input.
4. **Host transport**: implement private IPC or authenticated loopback HTTP facade for existing synchronous `resolve`, `archive_sdk`, `submit_result`, and replay. Resolve needs frozen assignment/context + verified principal. Archive/submit preserve exact raw/SDK bytes (binary or base64 with bounded size), native tool receipt identities and snapshot read set, then invoke the actual P03 ArtifactStore / P04 ResultCommitter in controller. Return only typed committed receipt. No invented ResultReceipt, evidence or successful callback. Restrict per-Run authority and current gate checks; allow only trusted Supervisor to reconcile already produced late bytes, not execute new model/tool actions. Allocate transport module ownership to SOL/Dirac; core will not touch worker_host/runtime/entrypoint/wire.
5. **Observation settlement**: new P10 Reconciler accepts validated existing P05 ExecutionObservation derived from trusted OS guardian, never from Worker result text. `persist_results(run)` must reconcile existing actual M1 raw output first, then P05.record_observation updates execution/capacity. Missing raw transport remains an explicit M2 blocker; not a reason to fabricate process observations or release capacity.
6. **Integration window**: Ptolemy may run Node-only harmless child/private temporary ports once source CORE_READY. PG/actual platform HTTP and final same-MAF/Scheduler/P05 integration require main's exclusive shared window after Dirac/Archimedes release. K8s user authorization exists, but core creates no resources. Main must identify a unique vNext test namespace, arm64 digest-pinned images, frozen config and cleanup ownership before SOL creates resources. Existing docker-desktop readiness does not prove Pod/isolation or M2 acceptance.

No tests/checks/evidence executed by core. These are concrete assembly requests, not a new Plan or schema/wire decision, and do not block independent Node inbox/spawn/control implementation after SOL first RED.

Concrete source findings sent to main: current P09 Scheduler INSERT does not set `agent_run.pod_uid`, while existing P05 requires exact equality before accepting any process observation. Bind this through a trusted environment/controller producer; P10 does not fill a fake UID or edit P09 schema. Actual P06 `current_run` also requires persisted process/work running before model/tool request, so the child launcher must wait for host-confirmed P05 start admission before calling current MAF entrypoint. A subprocess existing does not bypass this barrier. The Node guardian can report birth while that launcher waits. M1 result/host transport must use Dirac's final principal-verification and raw-archive fixes, then real same-Worker M2 runs under main's shared window.

## Main-confirmed start barrier and narrow adapter request

Latest main decision: Archimedes owns the trusted receiver/runtime `pod_uid` binding and narrow propagation into admitted Run. P10 must not edit P05 or invent a UID. Start sequence is fixed: **spawn inert bootstrap (guardian observes its real OS birth) → real running receipt → existing P05 persists started/running → receive permit bound to that same Run/epoch/start operation → invoke existing `run_assignment`**. No early SDK/model request and no weakening P06. Each model/tool Gate still rechecks Task pause/revocation afterwards.

Requested private host adapter port, to implement only after Dirac's `verified_principal` / `token_verifier` / native wire ownership closes:

```python
# Runs in the minimal Worker bootstrap, before MafRuntime/Agent creation.
# Uses a fixed authenticated host channel provided by trusted deployment.
admission = await host_transport.await_start(assignment)
# Adapter verifies assignment, receiver, actual observed birth and freshness.
await run_assignment(
    assignment, host=remote_host, context=resolved_context,
    run_credential=restricted_run_credential,
    token_verifier=actual_token_verifier,
    model_gate_url=fixed_model_gate, tool_gate_url=fixed_tool_gate,
)
```

`await_start` is a request to a controller-backed adapter, not `can_run: true` supplied by the caller. It may wait or reject; on success it returns the authenticated server-derived binding containing exact `identity`, `start_operation_id`, `assignment_digest`, receiver/environment/pod identity, the actual `process.birth_id`, and persisted P05 observation ID/source digest. Server re-reads original P09 Assignment and P05 current Run/Work/Task under existing permission, requires the actual trusted started observation and active epochs, and derives the birth identity from that observation. It must not accept caller PID, fake receipt or caller boolean as evidence. Receiver shutdown, revocation, stale identity or unresolved start leaves bootstrap inert/terminated; there is no timeout fallback into SDK.

Transport owner chooses the existing authenticated private IPC/HTTP mechanism with main; this request does not introduce a product endpoint, new generated DTO, credential purpose, database table or new signing protocol. Do not give child controller keys or general DB access to satisfy the handshake. Preserve current M1 verified-principal/token-verifier checks. The same physical bootstrap process must enter the same installed Worker; success in a separate hosted SDK process does not satisfy M2. Ptolemy's Node-only harmless fixture remains physical-process evidence only and cannot claim this barrier or M2 integration has run.
