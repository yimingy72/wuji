# P03 round 1 — scoped re-review

Date: 2026-09-13. **Verdict: APPROVED for this scoped fix. Initial findings: 2 ADDRESSED, 0 NOT ADDRESSED. New breakage in the reviewed diff: 0 findings.**

Reviewed `59960377eb1bad6c5479c3c043db753ffb1dde14..d5cdc30e71f71272249dd28d92fc7230d9771103` in `/Users/yym1ng/Documents/ChatGPT/wuji/work/worktrees/vnext-maf`, branch `codex/vnext-maf`. Implementation is `c2a86e3a98895378532fff1427e9ee82420b4039`; `d5cdc30` publishes its evidence. Scope is the two initial P2 findings in [P03-review.md](P03-review.md), their fixes and breakage introduced by this diff. This is not another full P03/SDK/domain review or aggregate AC acceptance.

Read the initial review, [P03 brief](P03-brief.md), [main implementation contract](../../../docs/vnext/P03-implementation-contract.md), current project/stage context, relevant v2 Spec/Plan sections, [fix report](../../../docs/vnext/evidence/P03/fix-round1/report.md), [review package](P03-round1-review-package.md) and [line index](P03-round1-review-index.txt). The package matches the actual `git diff --unified=5`, and all 80 index entries match the package. No CodeGraph index exists in this worktree; navigation used the SDD index, `rg` and scoped source reads.

Read-only review apart from this requested report: no implementation changes, subagents, database calls/mutations, application test reruns, commits or pushes. The existing untracked `docs/vnext/P04-implementation-contract.md` was excluded and left untouched.

**R1 / initial finding 1 [P2] — ADDRESSED: actual evidence and lease mutation authority.**

- [uow.py:154](../../../packages/wuji-core/src/wuji_core/persistence/uow.py:154) sets `wuji.capture=true` only for evidence/capture/settle capabilities, a collector without the Agent role, and the applicable stored task permission. Ordinary write and snapshot transactions no longer inherit capture authority from a collector role.
- [schema.py:247](../../../packages/wuji-core/src/wuji_core/persistence/schema.py:247) guards the actual staged→sealed transition. [schema.py:261](../../../packages/wuji-core/src/wuji_core/persistence/schema.py:261) additionally requires in-scope tenant/project/task and a matching, nonrevoked stored binding for the current subject and the Artifact's actual `tool_attempt_id`. Lease INSERT/UPDATE/DELETE resolve the referenced Artifact and invoke the same guard; DELETE uses OLD. UPDATE uses NEW safely under the existing application grant, which permits updating only `expires_at`, not the lease's identity, target, owner or access columns ([schema.py:433](../../../packages/wuji-core/src/wuji_core/persistence/schema.py:433)). The binding/authority tables remain unavailable for application writes.
- UPDATE visibility remains available for snapshot publication's `SELECT ... FOR UPDATE`; a row lock does not invoke these mutation triggers. The pre-existing metadata immutability, GC authority and retention checks remain in the replacement Artifact function. Normal store seal still checks actual bytes and disposition before mutation.

The stored six-test run includes four negative cases, each executing seal, lease expiry and lease deletion as real nonowner DML. All **12 operations produced SQLSTATE 42501**, rather than silently matching zero rows. Agent write, Agent snapshot and collector snapshot errors appear at lines 1140/1144/1148 in their respective [Agent-write SQL](../../../docs/vnext/evidence/P03/fix-round1/runtime/d56c6da94a8e/postgres-events.jsonl), [Agent-snapshot SQL](../../../docs/vnext/evidence/P03/fix-round1/runtime/9a9f5fd0a079/postgres-events.jsonl) and [collector-snapshot SQL](../../../docs/vnext/evidence/P03/fix-round1/runtime/cea67badd51b/postgres-events.jsonl). The [revoked-binding SQL](../../../docs/vnext/evidence/P03/fix-round1/runtime/a63c6b5dcb7f/postgres-events.jsonl) records revocation at line 1064, capture still true at line 1120, then guard failures at 1146/1150/1154. This establishes that the capability flag alone is insufficient. Each case records a non-superuser, non-BYPASSRLS application identity, and checks unchanged staged state/lease expiry after the isolated rollback attempts. Preserved red SQL shows all three DML operations changing one row in each corresponding unfixed case.

The [positive SQL](../../../docs/vnext/evidence/P03/fix-round1/runtime/7d884c116150/postgres-events.jsonl) records bound lease renewal by UPSERT at line 1138, successful sealing and lease deletion, a reader snapshot's Repeatable Read transaction at 1225, three successful publication pins at 1314/1318/1322 and commit at 1324. Ordinary orphan GC tombstones at 1961 and marks body removal at 2036. The [focused test](../../../tests/vnext/test_p03_fix_round1.py:132) also asserts the retained bytes remain readable and the real evidence route returns 202. These positive paths cover the row-lock and GC compatibility directly affected by R1.

**Migration compatibility:** [migrate](../../../packages/wuji-core/src/wuji_core/persistence/schema.py:338) recognizes an existing sole `vnext_0001_p03` record and invokes the small authority upgrade inside its transaction. It replaces the existing Artifact trigger function, creates the lease guard/functions, restricts function execution to the application role, and appends `vnext_0002_p03_evidence_authority`. It retains the old record, grants and business rows; it does not recreate the existing schema. Both recorded heads make reapplication a no-op, and unknown head sets are rejected. The original trigger already targets the same function name/signature, so replacement activates the guard for existing P03 tables. Fresh fixtures record base→upgrade installation at positive SQL lines 335–350. **The pre-existing-schema dispatch/reapply branches were statically reviewed, not separately runtime-tested in the six-case run or this review.** No concrete migration incompatibility was found that required an additional test.

**R2 / initial finding 2 [P2] — ADDRESSED: synchronous ingestion offloaded as one operation.**

[http/evidence.py:49](../../../packages/wuji-core/src/wuji_core/http/evidence.py:49) reads the original body asynchronously, then awaits `run_in_threadpool(service.ingest, access, payload, original_json=original_body)`. The complete [EvidenceService.ingest](../../../packages/wuji-core/src/wuji_core/evidence/observations.py:22) runs inside that worker, including strict source parsing/digest, connection acquisition, Task/resource lock waits, Artifact file reads/hashes and transaction exit/commit. Only value objects and bytes cross into the worker; no open transaction, cursor, lazy database result or request-body reader crosses back. The service/store retain a connection factory rather than a shared connection; [UnitOfWork.transaction](../../../packages/wuji-core/src/wuji_core/persistence/uow.py:107) creates and closes it inside the synchronous call. The exercised [factory](../../../tests/vnext/support/postgres.py:361) opens a distinct real psycopg connection for each invocation. No connection/thread misuse was found in this change.

The [regression](../../../tests/vnext/test_p03_fix_round1.py:176) uses one AsyncClient/ASGITransport and the real composed application. A separate holder thread takes the fixture Task lock; an observer queries actual PostgreSQL blockers before the unrelated content GET begins. [Raw concurrency SQL](../../../docs/vnext/evidence/P03/fix-round1/runtime/eff450c665f2/postgres-events.jsonl), lines 1053–1055, returns ingest backend `48184` blocked by holder `48182`. The [native event sequence](../../../docs/vnext/evidence/P03/fix-round1/runtime/eff450c665f2/concurrency-events.json) records:

| Event | Monotonic ns |
| --- | ---: |
| Task lock acquired | 142832877934458 |
| Actual ingest lock wait observed | 142832902441875 |
| Unrelated GET fully read, HTTP 200 | 142832920665000 |
| Lock release requested because response completed | 142832920706541 |
| Task lock released | 142832921130791 |

The GET body matches the saved fixture bytes; ingestion subsequently returns 202. Thus this is actual request progress while ingestion is blocked, not a timer heartbeat or a request launched after lock release. The [red event sequence](../../../docs/vnext/evidence/P03/fix-round1/red-runtime/test_task_lock_does_not_block_0/concurrency-events.json) instead uses `finite_fallback`, releases the lock, then completes the GET with `before_release=false`; the preserved red assertion fails on that condition. This establishes the targeted regression, not a load/capacity SLA.

**Evidence verification and limits.** Fresh offline checks in this review confirmed:

- The [recorded candidate tree](../../../docs/vnext/evidence/P03/fix-round1/final.json), `7b7989c64eec9c72fc597cad3831d2268a561d7c`, equals `c2a86e3^{tree}`. Code/test/ops paths are unchanged between `c2a86e3` and review HEAD.
- All **74 manifest-listed evidence files** are tracked and match their recorded byte counts and SHA-256 hashes. [JUnit](../../../docs/vnext/evidence/P03/fix-round1/final-junit.xml) has six cases, zero failures/errors/skips, and agrees with the six-entry [test index](../../../docs/vnext/evidence/P03/fix-round1/test-index.json). [Saved stdout](../../../docs/vnext/evidence/P03/fix-round1/final.txt) reports `6 passed in 3.03s`, exit 0 in the run record. These are verified preserved results, **not newly executed tests**.
- All **five saved HTTP exchanges** (three final, two red) retain complete methods, URLs, headers and exact base64 bodies. Their Content-Length values and all ten HTTP blocks in the [complete reproduction package](../../../docs/vnext/evidence/P03/fix-round1/http-reproduction.md) match. Both GET bodies match the saved native input bytes. R1 remains a database authorization defect, not a claimed public HTTP exploit; its full SQL statements, parameters and responses remain attached through the native logs above.
- The diff changes no original P03 evidence files and does not introduce a new asset or endpoint. The old 53-test result remains historical and was not rerun. No broad SDK/domain/GC matrix was added. PostgreSQL 16.2 fixture evidence does not establish production-image or cutover compatibility.

The screenshot below was visually inspected and matches the saved R1 denial and R2 event summaries. Its [provenance](../../../docs/vnext/evidence/P03/fix-round1/screenshots/provenance.json) identifies a browser rendering of saved observations, not a live product screen or new execution.

![Preserved screenshot evidence for R1 and R2 at c2a86e3](../../../docs/vnext/evidence/P03/fix-round1/screenshots/fixes.jpg)

**New breakage: none found in scope.** Both original P2 findings are closed. Existing future-owner limitations and aggregate AC statuses remain as recorded in the original P03 review; this scoped approval does not promote them to passed.
