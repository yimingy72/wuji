# 本次试测的用户决定

2026-09-18，用户明确要求实现和执行首用交付包，并指定供应商 DeepSeek、模型 `deepseek-flash`。凭据由用户提供，正文仅在受信秘密配置处理；本文件不包含凭据。

用户后续明确：查看 [DeepSeek 官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing) 获取发布单价；本次测试金额授权无上限。此决定覆盖旧阶段要求仅采用公司网关价目的来源限制。授权金额与平台每个 Task 的有限金额配置分开：本次首轮工程配置采用每 Task USD 1.00、并发 1、每轮最多 12 次实际模型 attempt、每次最多 4096 输出 token、每轮最多 4 次目标读取与 10 分钟窗口；限制用于有界试验，不表述为用户指定的金额。

用户指定 `http://8.146.204.146` 为其自建测试靶场。范围为该单实例 HTTP 80 的安全读取，路径是入口定位；不授权网络扩散、凭据攻击、破坏、持久化或外站跳转。按用户原始请求，将获准实例的必要响应正文交给 DeepSeek 形成带依据分析。先完成自建无敏感夹具机制/真实模型验证，再执行现场；原始可能敏感内容存受限位置，公开证据脱敏并记录其影响。用户提供的额外管理凭据不用于本轮 HTTP 流程。

该决定是执行许可，不是已成功调用、已验证连接或现场通过的证据；实际结果另记。
