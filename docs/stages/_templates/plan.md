# 阶段 Plan

> 模板默认状态为 `draft`。对应 Spec 未批准或方案尚未获批时不得改为 `approved`；Plan 模式中不写仓库、不执行实现。

- 状态：`draft`
- 阶段：待填写
- 日期：待填写
- 对应 Spec：[`spec.md`](spec.md)
- 基准提交 SHA：待填写
- 批准依据：待填写；未批准时保留“无”

## 背景与承接

开始规划或从中断处恢复时，先读[项目背景](../../project-context.md)和仓库根目录的[协作约定](../../../AGENTS.md)，再读本阶段 Spec、已有 Plan / Acceptance 及其直接上游设计。前两份文件提供索引与持续约束，不在本 Plan 重复大篇背景。

- 背景来源：待填写；列出本阶段实际采用的用户反馈、阶段记录、设计文档、外部参考及对应版本或日期
- 框架复用：待填写；说明复用的现有框架、库、服务和仓库模块，以及不重复实现的职责
- 平台补充：待填写；说明 Wuji 为权限、范围、账本、证据、恢复或交付增加的领域边界
- 兼容影响：待填写；说明对现有契约、数据、运行环境、前后端、部署和恢复的兼容要求；无影响时写明依据

## 固定方案与接口决策

记录已经选定的实现方案、公共接口、数据流和失败语义。尚未决定的内容必须标为未决事项，不能交给开发代理自行扩大范围。

## 依赖与执行顺序

列出工具链、上游任务、外部服务、唯一迁移编号或公共契约 owner。说明哪些任务可并行，哪些必须顺序执行。

## 任务分工

| 任务 | 执行者与模型 | 分支 / 绝对 worktree | 允许修改范围与唯一 owner | 依赖 | 交付与验证 |
| --- | --- | --- | --- | --- | --- |
| TASK-01 | 待填写 | 待填写 | 待填写 | 待填写 | 提交 SHA、变更清单、检查结果与限制 |

开发代理使用 `gpt-5.6-sol` / `xhigh`，后续独立集中测试代理使用 `gpt-5.6-luna` / `xhigh`。默认开发阶段并发上限为两个开发代理和一个测试代理；集中测试按根 AGENTS.md 的单独限制执行。历史测试证据按实际模型保留，不回写为当前默认模型。

## 验证计划

| 验收 ID | 命令或人工入口 | 所需环境 / 夹具 | 证据 |
| --- | --- | --- | --- |
| S01 | 待填写 | 待填写 | 待填写 |

指定独立测试的候选 SHA 形成方式、代表性交互证据和文档/忽略范围检查。非破坏性边界与未覆盖项处理应与 Spec 一致。

默认只做覆盖本次主流程、直接受影响失败路径和必要权限边界的最小验证。每个开发任务的测试执行、等待、脚本排查与复跑由所有代理共享 `600` 秒总预算，不能因代理、提交或修复轮次重置；通过即停止，超出范围或预算的场景如实进入待测清单。文档、文案和小样式改动按影响做 diff 检查、构建或一次页面查看，不自动新增测试。

## 集成、修复与完成规则

说明主代理集成顺序、冲突负责人和最终验收门槛。修复运行源码、配置或行为后必须生成新候选 SHA 并复测受影响项。缺少实际被测 SHA 或证据时不得完成阶段。
