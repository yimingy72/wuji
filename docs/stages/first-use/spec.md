# 首个用户流程

状态：approved / implementation in-progress。依据：2026-09-18 用户明确执行交付包 `WUJI-FIRST-USE-SUBAGENTS-20260918-V2` 及其中 01/02/03 与 A1–A8 任务书。

在既有 Wuji Scheduler/Blackboard、Supervisor、MAF、Model/Tool Gate 上，从正式工作台创建一个新限定 Task，显式 start，获取获准实例正文，交给指定的 DeepSeek `deepseek-flash`，保存带依据的分析，并能 pause/cancel 与核对真实执行状态。创建、就绪读取与浏览器断连均不启动/停止后台执行。

完整入口 path/query 保留为定位，授权仍由 scheme/host/port 与当前平台许可决定。Reason 只读已有证据；Explore 仅用已发布工具。材料为不可信文本，源引用和表示摘要分别校验。原生会话版本不能静默变更。

用户凭据只在受信网关配置。真实模型额度/输入数据及现场目标沿用有效批准，缺项明确记录；合成机制证据、真实模型与内网现场不得互代。首用不等于完整渗透能力或所有 P 项 accepted。

接口/所有权：[合同映射](../../vnext/first-use/contract-map.md)。验收：源包 87 场景逐项映射实际命令、退出码、固定 SHA 和证据；相关 HTTP/UI 交付必须包含真实截图与完整脱敏请求/响应。
