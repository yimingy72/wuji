# 项目背景与约束收口 Spec

- 状态：approved
- 日期：2026-09-10
- 批准依据：用户确认背景复核与 AGENTS/索引/模板整理方案后要求“好的，继续”；应用已进入执行模式。

## 目标与行为

根 AGENTS 固化关键产品/架构边界和上下文恢复流程；新增背景索引，能定位跨工作树有效设计及真实阶段状态；修正过时模板和当前说明；Phase 1C 前置补录最新确认并保持待修订状态。

不改变业务代码、接口、数据库、运行环境或阶段验收结果；不启用模型、目标访问、Runtime 或流量控制。本次批准不授权下一阶段业务开发。

## 验收

1. 根规则和背景入口在主工作区可发现；索引区分已确认、候选、实现和验收。
2. 模板使用当前模型和共享600秒最小验证；旧历史证据保留原模型/SHA/结果。
3. 五场景、无路径ACL、默认匿名、模型配置职责、框架复用与凭据/预算边界没有遗漏；Phase 1C 未假称批准或完成。
4. 变更仅为明确列出的 Markdown，diff 无空白错误，新增本地链接可解析。
