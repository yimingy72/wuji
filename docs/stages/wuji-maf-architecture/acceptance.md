# Wuji MAF 架构评审与验收状态

- 日期：2026-09-12；设计状态：`review-draft`；用户评审：`pending`。
- 实现测试状态：`not-tested`；实现阶段批准：无。
- 对应 [Spec](spec.md) / [Plan](plan.md)。
- 静态核对基准：`1d73a767599732d9a53f81ad2cc553f4bf11d84e`，`codex/github-upload`。
- 被测试代码 SHA：无；本轮没有运行新旧业务链路。
- 执行者：当前会话主代理；不是独立测试或第三方架构认证。

## 本轮交付范围

整体草案已描述模块/部署边界、领域对象、持久调度、MAF 组装、模型/工具准入、Session/人工输入/审批、停止恢复、结果一致性、覆盖/Goal、证据、UI、场景和迁移。Plan 只分解后续阶段，不授权实施。

MAF 静态参考固定为 `3c670707766a8455da6491a9049cc9d575e019f0`；未安装或执行框架。现有 W1 机制验收继续引用原 [f12a46f 记录](../phase-2-web-assessment/acceptance.md)，不作为新 MAF 证据。

本轮实际文档检查：`git diff --check` 退出 0；Python 只读脚本核对 10 份变更/新增 Markdown 的 190 个相对链接，全部目标存在；3 份新增草案的代码围栏成对、无尾随空白或未填写占位标记。变更清单仅包含这 3 份草案和 7 份已有文档入口，没有业务源码、锁文件、迁移或运行配置变化。

上述仅为文档整理检查，不是 A01—A10 的通过证据。没有新的测试成果或资产接口验证，因此本轮不生成验证截图/HTTP 报文，不伪造对应证据。

## 未来实现验收

| 项目 | 状态 | 原因 |
| --- | --- | --- |
| A01—A10 | `not-tested` | 业务重构未启动，各项条件和证据范围见 Spec §16 |
| MAF 与现有模型/工具兼容 | `not-tested` | 静态接口核查不能代替集成验证 |
| 真实模型自主效果 | `not-tested` | 无本轮新增 USD 授权、无选定模型和效果实验 |
| 生产出口 / 外部目标 | `not-tested` | 延续 W1 封闭运行边界，需独立实现和环境证据 |

后续若产生测试报告，每个成果必须同时附对应单位 `screenshots/` 的截图链接和完整可复现 HTTP 请求/响应；保留关键内容、注明漏洞点和采集完整性，敏感原文受限交付。缺证据不标通过，不以模型总结代替实际报文。

本阶段保持待评审；文档提交与未来被测代码 SHA 分开，不预写文档自身提交 SHA。
