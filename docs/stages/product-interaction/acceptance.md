# 产品交互原型验收记录

状态：prototype-review-ready / partial。2026-09-10 用户已确认交互方案；可点击原型待用户评审。业务执行与 Agent 能力未开发，不是 Phase 1C 或平台集成验收通过。

## 版本与环境

- run_id：`product-interaction-20260910-01`。
- 产品方案来源：`70e2aae`；业务核查基准：`381ae3a2205965ad6aab1ce787d490d2c02839f3`。
- 构建对应代码提交：`f1e6fa0`，构建在提交前执行，提交未修改代码；集成候选：`2e35177f5eba8343d4ec21e62a800c3d29c800cc`。两者原型代码 diff 为空，只增加已审查文档与合并历史，因此未重复构建。
- 浏览器实测：集成候选 `2e35177`；Codex 内置浏览器，默认雾银主题。五套主题复用现有共享包，未执行主题矩阵。
- 开发代理：GPT-5.6 SOL / xhigh；主代理完成最终语义修正、集成和页面走查。未另启动集中测试代理。
- 主工作区 HEAD 保持 `381ae3a` 且干净；没有改动正式 4180 服务、数据库、Kubernetes 或 master。

## 检查结果

| 检查 | 结果与退出码 |
|---|---|
| 初次原型 build | 退出 1：隔离工作树未安装依赖，tsc 不存在；没有修改锁文件 |
| `pnpm install --frozen-lockfile --offline` | 退出 0，202 个包来自本地缓存 |
| 依赖准备后的 build | 退出 2：TaskList 未使用的 stateLabel 导入；定向删除 |
| `pnpm --dir spikes/frontend build` | 修正后退出 0，TypeScript 与 Vite 正式构建通过 |
| `pnpm exec antd lint spikes/frontend/src --format json` | 最终退出 0，4 个非阻塞 warning：3 个 InputNumber addonAfter 弃用提示；5 项配色 Select 关闭虚拟列表的通用性能提示 |
| `git diff --check` | 退出 0 |
| Vite 独立启动 | 189ms ready，监听 `127.0.0.1:4185`，strictPort；交付服务仍运行，不伪造退出码 |
| 浏览器连接走查 | 主代理通过，交互工具没有统一测试退出码，按下述实际 UI 证据记录 |

浏览器观测：

1. HTTP 四步创建：默认场景、对象与授权、匿名无模型配置、预算摘要；创建得到 DEMO-HTTP-1084，状态待启动，已观察 0/1、请求 0/20、证据 00。
2. 显式启动先显示排队，模拟环境就绪后进入运行；EVD-7A21 响应出现在检查器。
3. 运行中取消先等待停止回执，再显示已取消和清理待完成；接收清理回执后显示已清理。结果只记录 HTTP 观察及限制，没有漏洞结论。
4. Agent 示例切换黑板；追加“优先核对已有响应的依据”后可见；点击身份边界 Intent 联动 EVD-7A2C，包含公开入口、401 观察与未访问的排除路径。
5. 从确认页模拟创建响应丢失，通过页面刷新到恢复页；保持原 CMD-DEMO-0007 和 DEMO-REC-1007，待启动、外部调用 0。
6. 打开恢复任务后直接取消，结果显示未执行、0/1、0/20、无需清理、证据 00。

浏览器工具个别 AX 点击因视口变化未触发，随后改用已读取 DOM 的语义定位；Segmented 原生 radio 的 check 未触发，点击可见标签后成功。没有为此重跑整个流程。

共享检查预算按保守口径计 318 秒（包含此前开发代理约 21.7 秒 CLI 检查，以及主代理从首次 build 至最后页面核查的全部经过时间）。通过后停止，不用剩余预算扩大测试。

## 已知限制与延期

- 这是 mock 交互原型：任务列表为固定示例，新增任务与所有状态尚未组成持久化列表；浏览器历史状态也不是服务端权威快照。正式实现必须使用现有任务 API 和版本机制。
- 问题回答、Hint 仅内存保存；执行记录、证据、恢复命令均为固定合成内容。没有真实模型、网络调用、身份引用解析或黑板写事务。
- 黑板为最小列表与证据联动，完整多输入因果关系、冲突处理、认领/租约留待 Phase 2/3 的 Spec。
- 3 个 InputNumber 弃用 warning 留在组件迁移清单；小数据配色 Select 的 warning 不需要扩大性能测试。
- 本轮未覆盖：完整 Agent 四步创建、问题回答及暂停恢复全过程、环境繁忙与范围阻断的浏览器路径、全部主题/宽度矩阵。相关代码已静态检查，不能等同浏览器通过；交互确认后按受影响范围集中安排，沿用 GPT-5.6-luna / xhigh 及共享预算约束。
- 不复跑旧原型 12 项、Phase 1A/B、API、生命周期或故障注入测试。本阶段结果不能替代其证据。

## 本地入口与运行

原型：<http://127.0.0.1:4185/tasks>。工作树：`work/worktrees/product-interaction-prototype`。

启动：在该工作树执行 `pnpm --dir spikes/frontend exec vite --host 127.0.0.1 --port 4185 --strictPort`。当前本轮启动的服务会话为 57151；停止时只终止该会话的 Vite 进程（前台运行时 Ctrl+C），不要调用正式平台 dev-down。

本记录为实测后的独立文档提交；记录提交与上方被测代码 SHA 分开。新 worktree 无 CodeGraph 索引，采用直接读取；主工作区代码与索引未变，不为独立原型强制建立索引。


## 用户反馈后的设计修订（2026-09-10）

新增场景/出网/流量方案 `docs/scenario-execution-design.md`，并根据用户进一步指示删除路径级访问约束。主代理与 SOL/xhigh 子代理进行了静态审查；本轮仅文档 diff 检查（退出 0），无安装、构建、浏览器或集群测试。上面的原型测试结果仍对应原 SHA，不作为新设计实现证据。当前 4185 页面仍是旧原型，后续修改创建范围字段时一并删除路径白名单/黑名单式表述。


2026-09-10 后续六项运行规则与页面依赖补充：仅修订场景方案、Kali 流量证据方案及其架构引用；独立 SOL/xhigh 只读审查，主代理检查文档 diff，无构建、浏览器、网络探测或集群测试。页面与业务代码保持不变。新设计不沿用旧原型测试为已实现证明。
