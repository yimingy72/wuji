# vNext MAF v2 实施验收状态

状态：in-progress；日期：2026-09-13。代码起点 `1d73a767599732d9a53f81ad2cc553f4bf11d84e`。

用户已批准实施；原始 [75条验收定义](../../vnext/ACCEPTANCE.md)中的 not_run 是导入时状态，不能当作当前代码结果。实际执行证据将在本阶段按任务、命令和被测提交追加。此时没有产品通过结论。

2026-09-13 最新进度审核以代码 `6f6892a` 为截面，见[审核与后续安排](progress-audit-2026-09-13.md)。本次只核对既有记录，不重跑测试；下表区分历史切片通过、最新局部实测和完整产品未覆盖项。

该次定向审核发现：P08 reject 虽满足原调用拒绝零执行与两代恢复断言，最终 P04 回执却为 `rejected / INVALID_SCHEMA`，测试未检查结果接纳。该历史发现现已有A1修正和实测，见下文更新；审核原文不改写。`6f6892a` 的退出撤销SQL已通过A2增量升级与定向守卫检查，完整P08仍未验收。

用户认可下一批计划后的实际更新：

- A1：问题来自合成模型的拒绝最终正文不符合AgentPayload，测试未检查结果接纳。`5233f8c`修正夹具并补持久ResultReceipt断言；[最终原输出](../../../work/p08/a1-reject-result-2/stdout.txt)为1 passed / 32.60s / exit0，仍零ToolAttempt。首轮新增检查中的SQL歧义失败保留于原目录；测试时HEAD/dirty绑定以同目录文件为准，不冒称提交后另跑。
- A2：`9a83e64`接入0015/0016增量迁移；[原输出](../../../work/p08/a2-session-writer-exit-1/stdout.txt)为6 passed / 23.60s / exit0，覆盖已知旧0014升级及精确退出撤销条件；不是新增6项真实OS退出验收。
- B1：`37aa463`移除测试中重复安装已统一迁移locator的前提；[原输出](../../../work/p11/b1-control-api-2/stdout.txt)为2 passed / 1.75s / exit0，实际PG+签名HTTP路由。前一安装冲突失败保留；这些本地结果的永久成果包尚待收口。
- C0：生产核心`3bd7d8a`/`e89dea8`、0017迁移`890b540`及SOL消费者修复`d2beac6`；Remote 7 passed / 14.81s，另2项Pod许可/receiver数据库检查通过。已有[永久原始证据、截图、HTTP/SQL与源码绑定](../../vnext/evidence/P10/runtime-adapters/README.md)，归档提交`eccc155`；严格TLS、丢ACK保持unknown、错误身份拒绝等按包内范围通过。记录型PodClient不代表K8s运行。
- C1/C2：`9d42f5e`/`708accc`完成TLS与arm64部署装配；随后 `a47e198` 修正Worker生产依赖、`3e79cb9`修正合成模型完整endpoint、`2a0c7ee`/`bc0f0f1`补齐work kind与workspace初始化、`df5c926`固定单Work容量。fresh6实际K8s链路已通过：[永久证据](../../vnext/evidence/P10/k8s-c2-20260914/README.md)，代码源 `df5c926`，Pod UID `6db41752-9826-4687-aae4-b9cf5e640632`；Reason/Explore各1项完成、4次模型200、2次Kali读取、2次accepted Run、2个sealed Artifact/Observation链和2个P04结果提交。随后通过正式cancel及 `556b8f4` stop修复完成receiver禁用、容量归零和Pod删除。范围限单一合成机制Task，不扩大为完整P08/P12或生产验收。
- A3：固定memory输入与原生compaction继续实施；`a455363`增加固定输入来源，`cfb1c2d`/`7956660`补对应wire及生成类型。实际child首次因缺wire字段在resolve失败已保留；后续真实child验证仍在进行，纯API/生成检查不替代恢复验收。
- P13/P14 K8s浏览器读取：`a8eeb7a`将P13装入Runtime兼容入口，`24f984f`增加独立public `api` Deployment；`e8a5162`增加同源本地浏览器会话BFF与正式React Flow页面，`09ad034`收紧Secret组读取。最终定向检查10项、fresh配置2项和web typecheck/build通过；真实HTTP为匿名401→登录200→会话200→拓扑200→跨Task404→退出204→退出后401，浏览器画布/列表均显示真实Intent与Origin且无控制台错误。见[P13 API证据](../../vnext/evidence/P13/k8s-api-20260915/README.md)和[P14浏览器证据](../../vnext/evidence/P14/k8s-browser-20260915/README.md)；`8e0f30e`另将44180从临时port-forward改为Kubernetes `LoadBalancer`，5项清单检查和真实HTTP/浏览器通过，见[P14入口证据](../../vnext/evidence/P14/k8s-loadbalancer-20260915/README.md)。范围仍是本地只读机制身份。
- P15记录详情局部切片：`e3ce5c8`把当前TopologySnapshot与选择状态交给RecordPanel，按精确revision和同一snapshot读取P13 RecordView；3项Vitest、typecheck/build、Intent/Origin真实HTTP与浏览器点击均通过，见[永久证据](../../vnext/evidence/P15/record-detail-20260915/README.md)。ViewStream、历史选择和Layout CAS仍未实现。
- P15历史选择局部切片：`f55f2ab`读取受权snapshot目录并切换`mode=history`；2项Vitest、typecheck/build、目录/历史拓扑/历史RecordView真实HTTP和浏览器选择均通过，见[永久证据](../../vnext/evidence/P15/history-browser-20260915/README.md)。当前目录无续页，因此opaque cursor加载更多未实测；ViewStream和Layout CAS仍未实现。

- P15-L Layout CAS 垂直切片：`b465238` 增加 `GET/PUT .../layouts/{view}`、`LayoutPreference`/viewport wire、0018 个人偏好迁移与 RLS、`LayoutRepository` CAS、BFF 固定只转发该 Task 的 layout GET/PUT 及工作台读写/冲突处理。真实结果：`pytest tests/vnext/test_layouts.py` 5 passed（真实 PostgreSQL + 签名 HTTP）、`test_web_gateway.py` 3 passed、`contracts:check:v2` 通过、拓扑 Vitest 25 passed、Playwright 拓扑 5 passed、typecheck/build 通过；K8s 上登录→revision 51→PUT 200 revision 52→stale 409→GET 不变，浏览器拖动/缩放刷新后节点与 viewport 完全一致，跨 Task 404、缺/错 Origin 403、非法 If-Match/JSON/未知或重复锚点 422、非 layout 写路径不转发，领域计数与 outbox 不变；实测发现并修复 409 后自动回写缺陷（修复前同手势 41 次 PUT/提示消失，修复后仅 1 次 stale 409、0 次后续写）。见[永久证据](../../vnext/evidence/P15/layout-cas-20260915/README.md)。范围限本地机制身份与两个知识视图；ViewStream、重连/重置、Artifact 预览与规模目标仍未实现。

- P08-S Session/操作前沿收口：`93f2fc6`/`2366969`/`cc65fef` 关闭 P08 会话审批套件红色基线（产品缺陷：Host 恢复的批准回调无法证明原始批准身份，现在私有 `_approval_request_id` 存在时必须相等、缺失时回落公开原始批准 id 且同样必须相等，伪造值仍 fail closed），并把 P06 前驱映射改为按 attempt 唯一匹配 + fail closed；同时清掉三处过期期望（P03/P04 head 列表、冻结 S13 路由集、`ResultEnvelope` payload 断言改到真正执行拒绝的 `AgentPayload` 边界）与两处 fixture 漂移（`work_dependency` 依赖写入改到 control capability；P05 控制 fixture 按生产接 `SessionRepository`，等待输入经 `inputs.save_delivery` 在 control 事务内解析）。四项平台复审残留（F2/F3/F4/F5）现状核对见[永久记录](../../vnext/evidence/P08/reviews/P08-residual-closure.md)。真实隔离 PostgreSQL 完整可运行集合 **405 passed / 0 failed**；固定记忆、原生 compaction 真实 child（A3）与 GC/半发布、Pod 级 stop/restart 仍另行处理，P08 仍非完整 accepted。

- P11-C 创建入口（第一增量）：`d0204db` 实现 `POST /api/v2/tasks`——迁移 `vnext_0019_p11_task_creation` （`vnext.published_profile` 目录、`vnext.task_create_command` 幂等台账、`vnext.create_task(...)` SECURITY DEFINER）、`TaskService` 与 `create_task_router`，并装入独立 public API。真实结果：`pytest tests/vnext/test_task_creation.py` 4 passed（真实 PostgreSQL + 签名 HTTP，覆盖非运行态持久化、仅创建者受权、无领域行、幂等重放、同键异体 409、缺项目控制权、跨租户、匿名、缺幂等键、未发布/已撤销 profile、过期授权）；`contracts:check:v2` 通过；相关集合 409 passed / 0 failed。K8s：0020 之前的 0019 迁移 job 完成，镜像由 `d0204db` 重建，真实 operator bearer 下 201/幂等 201/同键异体 409、匿名直连 API 401、浏览器入口 405 不转发，数据库任务为 pause/ready 且无 claim/intent/outbox/run/work 行。见[永久证据](../../vnext/evidence/P11/task-creation-20260915/README.md)。仍未完成：owner 侧发布新 Task admission/容量/调度身份，以及 hold/pause/resume/cancel 到实际 Pod/child 停止的观察闭环。
- P11-C admission 与控制命令（第二增量）：`8d6c972` 增加 owner 侧 `publish_task_admission`（admission 配置 + 容量池绑定 + 调度身份模板 + Pod controller + 部署主体访问行，固定幂等、内容不一致即失败）。真实结果：`tests/vnext/test_task_admission.py` 2 passed（真实 PostgreSQL：发布后创建 Task 具备 admission/两个池绑定/enabled 模板与 controller/预期访问行，重复发布为 no-op，随后经 `ControlAPI` start 受理为 version 2 且进入 run/running 并落 `task.started`）；邻近集合 163 passed。K8s 上对已创建 Task 运行 owner Job 发布（3 个池绑定、5 个部署主体、模板与 controller、admission_config=1），再经 runtime 控制 API：start 202（重放同 receipt、旧版本 409）、pause 202、cancel 202，任务变为 cancel/quiescing/false/epoch4/user_cancel，事件 task.started + 3×control.applied；本次该 Task `agent_run=0`，因此**不作进程停止结论**。见[永久证据](../../vnext/evidence/P11/task-admission-control-20260915/README.md)。仍未完成：从部署配置自动推导 admission 行的 owner 工具，以及 cancel 真正停止 Pod/child 的观察闭环。

- P11-C Pod 级停止观察（第三增量）：真实 K8s 上 `cancel` 受理（202, revision 4）后 5 秒内删除运行中的 Task Pod（`wuji-task-v-ca604b6abddb91218b985ea232f4c239-a1`，uid 9cb99dcf…，agent/kali 均为 `8d6c972` 重建镜像），work 项变为 reason=stopping(user_cancel)/explore=cancelled，事件 `work.stop_requested`+`control.applied`；Task 保持 `cancel/reconciling/false/epoch3/user_cancel` 且 `execution_observation=0`，未伪造退出。同时清理两项环境债：四个 Deployment 的过期凭据以部署签名键重签（24h），以及 agent/platform/kali/web 全平面按 `8d6c972` 重建并滚动；fixture 授权窗口按 owner 身份刷新，receiver 补 `can_admit`（permit 前置）。**未关闭**：Pod 运行期间 runtime 持续 `runtime_dispatch_error`(DomainError)，worker 从未启动（agent 无日志、`agent_run` 停在 registered），因此本增量只证明「cancel 真的停掉 Pod + 不伪造退出」，不证明干净退出观测、MAF 子进程往返或 reason→explore 交接。2026-09-15 追加诊断（`56ba97d`/`b5cc4ca` 先补齐错误码与 pod-environment 状态日志）：停顿由两项设计内守卫与一项部署缺口构成——Pod 停止后 `scheduler_receiver.enabled=false`（无匹配接收者则不再投递）、runtime 配置固定 `execution_epoch=2` 而 cancel/owner-reset/resume 已推进代次（PermitDenied 正确拒绝），以及 `vnext.session_capability` 在本租户尚无记录（worker 会话路径的部署前置缺失）。环境已恢复到一致状态（fixture=cancel/reconciling、无 Pod、receiver 禁用）。二次跟进：以 owner 复位同代次后 runtime 确实创建了新 Pod，但 agent 容器立即以 `RECEIVER_IDENTITY_CONFLICT` 退出——持久 inbox 拒绝在同一 `runtime_attempt` 上更换 receiver 身份（正确的 fail-closed 守卫）。结论：停止后的代次不能原地重跑，新运行必须使用新的 runtime attempt（任务与 runtime 配置同步）+ 每次尝试独立的 agent 状态卷或显式归档旧 inbox；该 owner 工具与 tenant session capability 一并留待下一步。 根因已修：`905fd51` 让 `TaskRuntimeConfig.resource_names` 按 attempt 分代（同一代重启复用自身卷、新代次拥有独立 ConfigMap/Secret/卷），相关测试 29 + 22 passed。仍未完成：owner 侧按新代次渲染整套代次接线（任务 ConfigMap/Secret/PVC + task-agent/task-kali Service 选择器 + runtime pod_runtime + gates executors[].binding，见[attempt 身份接线](../../vnext/evidence/P11/pod-stop-20260915/raw/attempt-identity-wirings.txt)）并发布 tenant session capability，之后才能验证 assignment 到达 supervisor 与干净退出。见[永久证据](../../vnext/evidence/P11/pod-stop-20260915/README.md)。

- P11-C 创建后 owner 启动命令（第四增量）：`d5a246a` 交付 `ops/vnext/task_launch.py`——单一 owner 动作按固定顺序执行 `prepare`（在首次激活前用部署已发布的 worker profiles 定稿 definition、发布 admission/executor、经真实领域服务接纳初始 Intent）、`activate`（经公开命令路由 `start`，bearer 由挂载的部署签名键现签，已激活任务读回而不重发）、`wire`（渲染并应用本 attempt 的 ConfigMap/Secret/PVC、重指 `task-agent`/`task-kali` 选择器、刷新 runtime `pod_runtime`/`task_ids` 与 gates executor binding、滚动 runtime/gates 并等待 Pod 就绪）、`capability`（仅在**观测到的** Pod UID 上发布 tenant session capability）。真实结果：`tests/vnext/test_task_launch.py` 6 passed（真实隔离 PostgreSQL：definition 定稿幂等与激活后拒改、admission 授权/executor 绑定、Intent 接纳、capability 在 receiver 行缺失或 Pod UID 不符时拒绝）；邻近集合 8 passed。K8s 实测（Task `2abfdd57-7a0f-4cba-8ce4-2a57f751d0b0`，一次 Job）：`POST /api/v2/tasks` 201 → 四阶段全绿 `SuccessCriteriaMet`，`start` 202/revision 2/epoch 2，Pod `wuji-task-v-d92c6d1e6096f3f5b1e104da8a39a85f-a1`（uid 363d560a…，2/2 Running）由 runtime 创建，3 条 capability 绑定该 uid；随后 scheduler 接纳 reason 工作、runtime 投递 assignment、平台记录真实 `started`/`exited` 观测。见[永久证据](../../vnext/evidence/P11/task-roundtrip-20260915/README.md)。**未关闭**：MAF 子进程在 start 门 `Worker start was revoked`（`WorkerBridge._await_start` 尚未输出被拒谓词）；无工具调用的 run 无 `run_operation_settlement`，work 项停在 `reconciling/operations_unsettled`；一次投递结果为 unknown 时平台拒绝重投（即使受信 receiver 已答 `OPERATION_NOT_FOUND`）。本增量不提升 P11 验收、P12 完成或生产身份。

- 2026-09-16 外部审查（固定提交 `4a4fe61`）F01–F06 修复进度：**F02** 单条被拒/不可达操作不再中止整批，被拒项以有界分类保留待下轮；**F01** 新派发与旧 Run 核对分离——Pod 环境不就绪、receiver 停用、Run 已 exited 只阻止新启动，非终结 work 的既有 Run 仍经 `reconcile_pending()` 查询与结算（只读、绝不新建 Run）；**F03** 批次按 Task 轮转、任务内 keyset 只重排不过滤，消除稳定前缀饥饿；**F04** 布局 409 立即冻结写队列、重读失败不再重投旧 PUT（`apps/web` typecheck 通过，Playwright 回归待补）；**F06** RemoteHost 非 200 带上有界 HTTP 状态码（正文不在流式上下文读取，避免上一轮回归）。缺陷项 **F05**（CI）已写好 `.github/workflows/vnext.yml`（v2 合同 + vNext 套件 + web typecheck/build），推送被 GitHub 凭证 `workflow` scope 拒绝，仅在本地 `aa6a71c`。真实结果：`tests/vnext`（除需 `kubernetes` 包的 `test_pod_runtime.py`/`test_configure_refresh.py` 无法在该 venv 收集外）**430 passed / 0 failed，425s**，其中含 F01 真库集成回归（停用 receiver 后旧 Run 仍被核对、无第二次 PUT、容量不释放）。仍未完成：F01/F02/F03 的双 Task 集成回归、F04 的 Playwright 用例、F05 的远端生效与 required checks。

- 2026-09-15 全量进度审核（对照 Spec/P00–P20/75 AC）：[progress-audit-2026-09-15.md](progress-audit-2026-09-15.md)。结论：P15-L 交付、P08-S 红色基线清零与四项残留关闭、P11-C 创建/准入/停止观察有真实 K8s 证据；执行闭环、P12、ViewStream、P16–P20 仍未完成。

| 范围 | 状态 | 说明 |
| --- | --- | --- |
| P00 基线与来源 | accepted（本项） | 基线 c9871a6；源文件保留与实际环境已核对，GPT-6/xhigh 审查无发现 |
| P01 SDK 探针 | accepted（局部能力） | [当前报告](../../vnext/P01-report.md)；SDK13项@8c3fa9c，watchdog3项@1d77954，离线渲染4项@1598ea2；[最终范围复核](../../vnext/evidence/P01/review-round2.md)关闭所有发现，后续产品 Gate 独立 |
| P02 合同与测试底座 | accepted（本项） | [原报告](../../vnext/evidence/P02/report.md)、[修复](../../vnext/evidence/P02/fix-round1/report.md)、[最终范围复核](../../vnext/evidence/P02/final-controller-review.md)；后续完整 AC 仍 partial |
| P03 规范持久化与证据 | accepted（本项） | 53项@dcf5cc2；修复6项@c2a86e3；[范围复审](../../vnext/evidence/P03/fix-round1/review.md)关闭2项P2；完整AC仍按下游分工 |
| P04 知识接纳与评估 | accepted（本项） | [报告](../../vnext/evidence/P04/report.md)；24项原候选、6项可见性、3项新鲜度定向结果；[范围复核](../../vnext/evidence/P04/review-fix-round1/controller-review.md)关闭F1/F2 |
| P05 控制与容量 | accepted（本项） | 代码 `3102579`；[修复报告](../../vnext/evidence/P05/fix-round1/report.md)与[主控制器定向复核](../../vnext/evidence/P05/fix-round1/controller-review.md)关闭初审 F1/F2；复用实施者已采集 3 项定向证据，不扩大为完整 Runtime 集成验收 |
| P06 模型/工具准入与累计账本 | accepted（本项） | 基础 `f32678d`，修复至 `78095e6`，迁移头 `vnext_0009_p06_request_write_guards`；[最终报告](../../vnext/evidence/P06/full-candidate-6/report.md)记录 35 项 P06、1 项 P04 权限消费者与合同检查通过，[独立静态复审](../../vnext/evidence/P06/full-candidate-6/independent-review.md) PASS；完整 MCP/P08/P10/P16/P17 与真实 LiteLLM/外部目标仍 not_run |
| P07 ContextBundle pure slice | complete | D10 SOL/xhigh Raman `01a0989e` 执行目标测试，8 passed；代码/测试 `491ec0144ae63724e3696e9b47f259ea350ea4eb`，[证据](../../vnext/evidence/P07/context-pure-green)提交 `e22f7ce09c8aa052862440bca769506f4ee89ffa`；仅证明纯函数上下文构建及直接输入边界 |
| P07 M1 hosted runtime slice | reviewed / passed（本切片） | 修复代码 `5cfd581129c7991deaddaad9bfdaf8b4ee34b140`、[证据](../../vnext/evidence/P07/M1/review-fixes/README.md)提交 `57ec12ea2ada7acf7fe6a0ac7ac98b40e7c66012`；M1 5 项与直接受影响 P06 native consumer 2 项通过，[初审](../../vnext/evidence/P07/M1/reviews/P07-M1-review.md)的 2 个 P1/1 个 P2 已由 [PASS 复审](../../vnext/evidence/P07/M1/reviews/P07-M1-rereview.md)全部关闭；只确认 hosted M1 |
| P07 完整 SDK/runtime 合同 | partial / not accepted | M1 不覆盖原生审批、压缩、Session 恢复、完整负控、Supervisor/process truth、Scheduler、Task completion、Kubernetes/Pod 或真实模型效果；完整 P07 未 accepted |
| M2 host/child transport | reviewed / passed（限定切片） | [永久证据](../../vnext/evidence/P10/M2/README.md)提交 `bdee147`：原四项 P2 全部关闭，真实 Scheduler/Outbox→Node→MAF child→Gates→P03/P04、当前/历史补交和半提交恢复已按分 SHA 验证。最终 current 场景 `d61cb91`（产品 `4268b6a`、测试 `0f545ba`、冻结0013）1项/11.55s；其余撤销、Pod、profile、基础与真实 Thread 检查复用各自记录，不合称一次全量运行。r1–r6 FAIL 保留。范围仅 hosted Explore，不覆盖完整P07/P10/P08、其他work kind或Kubernetes |
| P09 Scheduler 已验证切片与定向复审 | reviewed / partial；非完整 accepted | 基线 `a3f7a95eacce20cbdeeaf654ea8f86064222ba0c` 原 31 项、直接消费者 4 项、共享容量竞态 1 项；修复 `e0a10b586905ed705b0d7f520bda6f6f96b26cac` 提交前 A/B/C RED 3→GREEN 3、0010→0011 升级 1、synthetic receiver UID 复制 1。[永久证据与缺口](../../vnext/evidence/P09/README.md)、[machine index](../../vnext/evidence/P09/index.json)、[初审](../../vnext/evidence/P09/reviews/P09-review.md)和 [Dalton 独立定向复审 PASS](../../vnext/evidence/P09/reviews/P09-rereview.md)保留。其后真实 Outbox/child 的限定 Explore M2 已通过，按上行证据；P08 两代 child 输入恢复按下行局部实测。其他非知识事件、完整 failure/retry 与各 work kind 仍未全验收；旧逐查询输入缺口不追写成已有 |
| P08 会话与审批 | in-progress / runtime partial；not accepted | 后续产品/测试已至 `dd400bf`、`6f6892a`。两代真实 Node/Python child 的 approve/reject 为 2 passed / 60.37s / exit 0，运行时 HEAD=`dd400bf` 加保存的 diff，随后提交 `6f6892a`；旧混合 cold 批 5 项与权限 3 项分别保留原范围。真实0014/native boundary已有局部结果。见[本地原始入口与缺口](progress-audit-2026-09-13.md)：压缩、真实 child 记忆、原子失败、并发/CAS/旧 writer、半发布/GC、旧批准控制边界等尚待收口；候选未发布 verified。`f4c3b76` [历史四残留复审](../../vnext/evidence/P08/reviews/P08-platform-rereview.md)不代表最新源码结论，也不能被两项成功路径整体关闭；最新证据仍待永久归档 | 2026-09-15 P08-S：会话审批套件红色基线已清零、四项复审残留现状核对完成（[记录](../../vnext/evidence/P08/reviews/P08-residual-closure.md)），仍未整体 accepted。
| P11 控制 API 与恢复集成 | runtime partial；not accepted | `64f4c29`审批路由已被P08真实HTTP消费；`f6cad17` Task/Work command API与受限Work locator已通过后续A2统一迁移/B1两项真实PG+签名HTTP路由检查。2026-09-15本地只读浏览器会话已实测，但它不提供生产OIDC、项目选择或写命令；正式创建/项目权限、完整hold/pause/cancel恢复与失败域集成仍待完成 | 2026-09-15：`d0204db` 交付正式创建入口、`8d6c972` 交付 owner 侧 admission 发布与控制命令受理（P11-C 两增量，见上）；K8s 上已创建 Task 在 `agent_run=0` 前提下 start/pause/cancel 均受理，控制命令到真实进程停止的观察闭环仍未验收。
| P12 可信完成 | not implemented / not accepted | 已有冻结合同与前置服务；可信 precheck、quiescing/settlement、ReportCommit/Delivery 与实际完成协议尚未交付。P10真实退出与P04结果接纳均不替代Task完成 |
| P10—P12 完整机制验收 | partial / not accepted | 真实 Outbox/child 限定 M2 与 fresh6 K8s Explore 切片已通过；完整控制/恢复/可信完成、浏览器入口、其他 work kind 与部署故障矩阵仍待实际集成，不扩大已有切片结论 |
| P13 受权图投影与持久视图 | backend reviewed / M4 partial | core `3015ca6`、P04 port `592bed2`、0012迁移与测试 `44ddc68` 的[真实结果](../../vnext/evidence/P13/runtime-green/report.md)为 12 项通过；两项P2由`69e3a1d`关闭并完成独立复审。`24f984f`后续将同一ProjectionRepository装入独立K8s public API，真实Bearer 200、匿名401、live/history持久化见[API证据](../../vnext/evidence/P13/k8s-api-20260915/README.md)。记录面板与历史选择已由 P15 切片补齐，Layout 由 `b465238` 切片补齐；ViewStream 及后续 P12 类型仍未验收，故不标 M4 全过 |
| P14 TopologyFlowCanvas fixed DTO slice | reviewed / partial | 原16 Vitest、2个Chromium和复审结论继续有效；`e8a5162`后续把组件接到真实P13 API和本地服务器会话，K8s浏览器实测画布/列表显示两个真实节点，跨Task拒绝、退出失效、截图与完整HTTP见[P14证据](../../vnext/evidence/P14/k8s-browser-20260915/README.md)。记录详情、历史选择与 Layout CAS 已由后续 P15 切片补齐；生产OIDC、ViewStream 与规模p95仍pending/not_run，不是M4闭环 |
| P15 ViewStream、历史与浏览器 | record/history partial；not accepted | `e3ce5c8`已实测同一snapshot的Intent/Origin详情；`f55f2ab`已实测目录和精确历史选择，[P15证据索引](../../vnext/evidence/P15/README.md)固定HTTP与截图。ViewStream重连/reset、历史目录续页、Layout CAS、Artifact预览和规模目标仍not_run |
| P16 数据治理与交付 | in-progress（P16-A 已交付，AC-069 局部） | 报告交付切片已实测：迁移 `vnext_0026_p16_report_delivery`、`ReportDeliveryService`、三条产品路由与工作台面板；AC-069 的正例/反例有真实 K8s + 浏览器证据，但保留/GC/purge（AC-066/067）与审计分离（AC-068）仍未做，HTTP 交付适配器未实现 |
| P17—P20 开发与机制验收 | not_run | 按依赖执行，未验证不标通过 |
| 真实模型效果 | not_run | 需明确模型/数据及新增 USD 额度 |
| 生产切换/旧数据删除 | not_run | 需独立明确授权；开发不隐式实施 |

- P11-C 往返闭环（2026-09-16，代码 `9eb1a8a`）：创建入口创建的 Task 经 `prepare → activate → wire → capability` 进入真实 runtime attempt；同一次实测中 `reason` work 由 Scheduler 接纳、MAF child 完成 2 次模型调用与 1 次工具调用、结果被平台以 `received/accepted` 接纳并收口为 `done`，gates 同时记录了 `permits/check 200`。同一 attempt 的 `explore` work 工具调用被 `429` 拒绝后停在 `reconciling/operations_unsettled`，属未关闭缺陷。见[证据包](../../vnext/evidence/P11/task-roundtrip-20260916/README.md)；范围仍限本地单 Task 机制切片，不提升 P11/P12/多 Task 验收。
- P06 零操作 run 结算（2026-09-16，代码 `27673ab`）：`POST /internal/v2/tool-settlement` 与迁移 `vnext_0020_p06_run_settlement_close` 让 run 用自身凭据关闭自己的操作集，状态仍由平台按持久记录（未终态 tool attempt、inflight model call、未释放 resource）计算；无工具尝试的 run 不再永久停在 `reconciling/operations_unsettled`。真实 K8s 实测 Task `0c33b0bf-…`：两个 run 均 `settled`（其中零工具尝试者 basis `run_closed_operation_set`），work item 分别以 `process_failure` 收口；gates 两次 `tool-settlement 200`。同轮暴露的新缺陷（已接纳结果仍以 exit 1 收口、429 以 SDK 函数失败呈现、模型门连接错误）记入[证据包 §7.1](../../vnext/evidence/P11/task-roundtrip-20260916/README.md)。
- P10 结果归属（2026-09-16，代码 `7fc993a`）：supervisor 只在该 attempt 的进程处于终态时恢复其产出的 result/sdk，运行中的 child 独占自己的提交；此前 supervisor 的一次 query 会先以 receiver 身份提交同一份字节，使 child 自己的 `submit_result` 被 `INPUT_DIGEST_CONFLICT` 拒绝并以 exit 1 收口。实测 Task `78dbcb53-…`：`reason` 提交 writer 为 `run.worker:…`、work item `done`；`explore` 在同一 attempt 内因 workspace 资源互斥被有界拒绝（`LIMIT_BLOCKED`）后以 `process_failure` 收口。见[证据包 §8](../../vnext/evidence/P11/task-roundtrip-20260916/README.md)；Node supervisor 套件 22 项通过并已进入 CI。
- F04 浏览器回归补齐（2026-09-16，代码 `20ec88a`）：新增 `layout-reload-failure` 夹具与两项 Playwright 用例——409 后重读也失败时被拒 PUT 不重排队（PUT 恒为 1 次）、冲突未解决时新手势被拒、只有显式重读成功才解冻并允许下一次保存；冲突未解决时切换 Task 不产生任何写入。`playwright test --config playwright.topology.config.ts` 7 passed，该套件已加入 CI（连同此前的 Node supervisor 套件）。
- 新派发隔离取证（2026-09-16，代码 `de623e7`）：owner 命令新增 `--phase intent`（复用初始 Intent 的签名控制主体路径、无需 attempt binding、按幂等键可重复），对 ready 的 Task A 接纳一条后续 Intent 后，调度器派生出新的 explore work（`bd62d4c0|explore|leased|9e574eb0|65c758b2`）并**新建 attempt 2 上的 Run**（`9e574eb0|2|registered`），`run.dispatch_requested` 与 attempt 2 的启用 receiver 均在位；同一时段 runtime 仍逐条报告两个 stopped Task（`permit_expired`/`task_not_runnable`）且从不报告 A，循环继续。**未完成**：该新 Run 尚未执行——它停在 `registered`，runtime 持续 `runtime_dispatch_cycle observed:2 states:{unknown:2}`，即被查询但平台尚未取得可信观测；这是下一步要定位的点，不冒称已完成。见[证据包 §9](../../vnext/evidence/P11/two-task-regression-20260916/README.md)。
- 双 Task 隔离取证的状态（2026-09-16）：集群现处于"一个 ready + 两个 stopped"配置——Task A（attempt 2，`run/running`）从未出现在 runtime 的 not-ready 列表，而 B（`permit_expired`）与 C（`task_not_runnable`，已取消）每轮被逐条报告且循环继续。**仍缺**：向 ready 的 Task 派发**新** start。原因是该 Task 两个 work item 都已终态、没有新工作可派发，而造新工作需要权限——operator 在本 Task 上只有 `can_read/can_write/can_control`、没有 `can_admit`（`pod-controller`/`receiver`/`scheduler` 才有），owner 命令虽已有经验证的 `admit_initial_intent` 路径但未暴露"再接纳一条 Intent"的阶段。最省的合法夹具是给 owner 命令加一个"额外接纳一条 Intent"的阶段（同一条已证路径、同一签名控制主体、新的幂等键）；改为给 operator 授 `can_admit` 属于产品权限决策，不用于测试夹具。见[证据包 §8](../../vnext/evidence/P11/two-task-regression-20260916/README.md)。
- attempt 滚动闭环（2026-09-16，代码 `3be42f2` + `005fac0`）：滚动后还差两块，均是实机暴露：固定 executor 注册表拒绝指向新 attempt 的 receiver/environment（现仅在"存储指向更早 attempt + 新文档指向当前 attempt + 更早 attempt 确实已死"三者同时成立时**取代**该行，否则仍 `INPUT_DIGEST_CONFLICT`）；`activate` 复用了首次启动的幂等键（同键异体被拒），现滚动后的 attempt 使用 `task-launch-start-<task>-a<n>`。真实结果：对卡住的 Task A 普通 `--submit` 四阶段全绿（`SuccessCriteriaMet|1|`），新 attempt 真正执行——`2|f|run|running|3|4`、两个 Run 均在 `runtime_attempt=2`（一 accepted 一 incomplete）、`reason|done`/`explore|failed|process_failure`、事件 `task.attempt_rolled(4,→2)` 后 `task.started(5)`、Pod `…-a2` 2/2 Running。单测：`test_task_launch.py` 11 passed。
- attempt 滚动（2026-09-16，代码 `86995da` + 接线修复 `19a1a8c`）：owner 命令新增带守卫的 `--roll-attempt`/`--phase roll`，拒绝未激活、仍可运行、仍有未退出 Run、仍有未释放容量、仍有启用 receiver 或已关闭的 Task；允许时把 `runtime_attempt` 加一、清空 `activated_at`、置回 `pause/ready`、递增 `control_version` 并写 `task.attempt_rolled` 事件（`execution_epoch` 留给随后的 start）。单测：`test_task_launch.py` 10 passed。实机：对卡住的 Task A（`86a2a7f2…`，窗口已过、无 Pod、容量已释放）执行滚动后读回 `2||pause|ready`、`control_version 3`、outbox `4|task.attempt_rolled`——滚动本身成功；随后 `prepare` 在 `publish_admission → register_executor → _insert_fixed` 报 `INPUT_DIGEST_CONFLICT`（`kali-workspace-v1` 的 fixed 注册仍绑 attempt 1 的 receiver/environment），这是滚动路径的下一处待修，Task A 现停在一致的 `runtime_attempt=2 / pause / ready`。另：本轮先暴露 `--roll-attempt` 被加进 binding 文档而非 phases options（实机上滚动静默未执行），已由 `19a1a8c` 修正。
- Task attempt permit 的拒绝原因（2026-09-16，代码 `7194df2`）：`PermitDenied` 增加有界小写 code（`permit_missing`/`permit_expired`/`permit_binding_mismatch`/`permit_unavailable`/`task_not_runnable`/`start_event_missing`/`start_definition_changed`/`start_command_unavailable`/`pod_deadline_exceeds_limit`），runtime observation 携带该 code，运行循环把它与 `reason=permit_revoked` 一起打印；消息文本仍只留在内部。真实结果：新镜像滚更后，集群内四个 Task 分别报 `permit_expired`（窗口过期，含无法重试的 86a2a7f2）与 `task_not_runnable`（被取消的 6ffd59cd），循环同时继续 dispatch 周期；`test_runtime_permit_codes.py` 3 项（以桩替换 Kubernetes quantity 解析）+ 受影响套件 39 项通过。**未覆盖**：attempt 滚动本身仍未实现，重试失败 launch 依旧需要新 attempt。
- 多 Task runtime host（2026-09-16，`f16fa69` 起四个提交）：runtime 配置改为有界 Task 列表（旧单 Task 键仍可读并在下次 wire 迁移），`PodEnvironment` 逐 Task 独立 ensure，运行循环只把**新派发**限制在本轮 ready 的 Task 上、核对仍覆盖全部授权 Task；task_launch 改为合并（task_ids 取排序并集、只替换本 Task 的 pod_runtime 条目与 gate executor 条目、profile 按 (ref,revision) 合并并对同键异体报 `INPUT_DIGEST_CONFLICT`）；gate host 的 executor/collector 改按 `(tenant,project,task,executor_ref)` 归属。真实结果：`runtime-config` 已迁移为两 Task 列表、`gates` 以两条 Task 绑定 2/2 Running、运行循环每轮逐 Task 输出 `runtime_pod_environment` 后仍继续 dispatch 周期；单测 35 + 43 通过。实机（B `3aa77fba…` + C `6ffd59cd…`）：**两个 Task Pod 同时 2/2 Running**，各自 reason 结果被接纳、work 达 `done`；对 C 发公开 `cancel` 命令（202、revision 3）后 C 变为 `cancel/quiescing/execution_allowed=false/user_cancel` 且其 Pod 被删除，**B 保持 `run/running`、Pod 仍 2/2 Running、work 与 6 条容量不变**，运行循环继续按 Task 逐条报告停用者。**遗留缺陷**：attempt 的 permit 以 `activated_at + max_elapsed_seconds` 到期，launch 在 `wire` 之后失败会耗尽该窗口，重试只会得到 `permit_revoked`/`POD_NOT_READY`——需要重试时滚动到新的 runtime attempt，owner 命令尚未支持。
- 工作台入口稳定性（2026-09-16，代码 `9e42f42`）：`wuji-web` 的 gateway readiness 探针每次在容器内起 Python 且只给 1s（探针自身也 1s），在负载节点上会真超时，导致 `Service/wuji-web` 的 Localhost 端点被摘除、`http://127.0.0.1:44180/` 间歇返回空响应。改为请求 3s、探针 5s、周期 10s、连续 3 次失败才摘除；实测连续 6 次请求（间隔 4s）全部 200。**未覆盖**：探针是可用性配置，不改变任何平台语义。
- 阶段 B 分支 1 实机往返（2026-09-16，代码 `9e5bfb6`）：公共创建入口 `201` → owner 四阶段（`SuccessCriteriaMet`）→ 真实 runtime attempt 与 2/2 Task Pod → `reason` 结果以 `received/accepted` 收口为 `done`（writer `run.worker:bbfaf3d7…`），`explore` 被有界 `LIMIT_BLOCKED` 拒绝后以 `process_failure` 收口，两条 Run 分别以 `durable_tool_attempt_receipts`/`run_closed_operation_set` 结算，6 条容量全释放。同轮暴露并修复：工具门是**缓冲**响应，流式读信封会抛 `StreamConsumed` 使拒绝丢码——`9e5bfb6` 拆出 `error_code_from_bytes` 供缓冲路径使用，实机 child 有界行变为 `code=LIMIT_BLOCKED, status=429, error=MiddlewareFailure`（失败与修复两轮原始记录都保留在证据包 raw/raw2）。见[证据包](../../vnext/evidence/P11/stage-b-roundtrip-20260916/README.md)。**未覆盖**：分支 2/3 仍待各自夹具；runtime host 仍是每部署单 Task，因此“另一 Task 不被连带停止”尚未实机证明。
- 2026-09-16 镜像重建与滚更（代码 `5675761`）：`python3 scripts/vnext/k8s.py build` 与 `publish` 产出 platform `127.0.0.1:56615/wuji-vnext-platform@sha256:789f2360…`、agent `…wuji-vnext-agent@sha256:b5350b4a…`、kali `…wuji-vnext-kali@sha256:c9e6c389…`（source_revision `567576120897fcdc3684312638e81d2c5d29097d`）；`kubectl set image` 后 api/runtime/scheduler/gates 四个 Deployment 均 rollout 成功并运行该摘要，`http://127.0.0.1:44180/` 返回 200。**未覆盖**：本轮未用新镜像重跑 Task；`wuji-web` 的 gateway sidecar 与 gates 的 `synthetic-model` 容器仍为旧摘要（本轮未改 web/loopback 夹具），双 Task 实机回归仍待运行。
- 工具拒绝与模型门失败的命名（2026-09-16）：被平台有界拒绝的工具调用仍逃出 MAF 原生循环（不把容量拒绝变成给模型的重试邀请），但现在该失败带平台发布 `code` 与 HTTP 状态，操作者信号从不可读的类名变为 `code=LIMIT_BLOCKED, status=429, error=MiddlewareFailure`，且不携带响应正文；SDK 因悬空函数调用发起的后续模型请求按因链分类为 `ModelGateTransportError`/`ModelGateRejectedError`。真实结果：`tests/vnext/test_maf_child_transport.py` 15 passed（含两个新增用例，一个有界code/状态与不泄文本，一个因链分类），受影响 child 套件 43 passed。**未覆盖**：尚未在真实 K8s Task 上复取新的 stderr 行，也未改变 Run 的终态语义。
- Reason 重试预算归属（2026-09-16，决定 D20）：`ReasonLedger.fail` 不再借用 SPEC 10.2 的模型格式修复预算 `repair_attempts`，改用新的发布限额 `reason_retry_attempts`（ExecutionLimits 可选整数，默认 0），并保留任务级 `max_reason_runs` 上界。Control settle 仍把该次尝试如实标为终态 `failed`；退避到期后 P09 以 `reason:{generation}:retry:{failure_count}` 登记身份不同的新 Work item 再准入，既有已终结 Work 不被复活。真实结果：新增 `tests/vnext/test_scheduler_generations.py::test_reason_retry_budget_leases_a_fresh_work_item_then_blocks_when_exhausted` 在真实 PostgreSQL + 生产 Scheduler/Control/Trigger 上通过（`repair_attempts=0`、`reason_retry_attempts=1` 时租到新 Work item 与新 Run；耗尽后 `retry_at` 清空、`blocked_reason=reason_retry_exhausted` 且 block 行含责任角色；同一用例对旧公式失败，证明它区分修复）；`test_scheduler_generations.py` + `test_contract_shapes.py` 94 passed。**未覆盖**：格式修复（SPEC 10.2）仍无消费者，真实模型效果与多 Task 重试仍待测。
- 阶段 B 分支 3（取消，2026-09-16 实测）：对已创建且运行的 Task 通过公开命令路由发 `cancel` → `202`、control version 3、`desired_state=cancel`/`observed_state=quiescing`/`execution_allowed=false`/`close_trigger=user_cancel`；Task Pod 被删除、runtime 记录有界 `permit_revoked/stopped`；两个 run 保留真实 `exited` 观测（不伪造退出），已终态 work 不被改写，6 条容量预留全部 `released`。**未覆盖**：真正在 run 执行中投递取消（夹具 run 在 Pod ready 后 ~15s 内结束，且 Service port-forward 被 wire 的滚动打断）；该半支由 P05/P06 准入套件覆盖并保留在延期清单。见[证据包 §9](../../vnext/evidence/P11/task-roundtrip-20260916/README.md)。
- attempt 凭据到期导致孤儿 Run 与滚动死锁（2026-09-16 诊断，代码基线 `34b7802`，证据提交 `89f7e0d`）：Task A attempt 2 的第三个 Run `9e574eb0` 在 receiver bearer 到期（`2026-09-16T09:29:23Z`）后派发，三次 `PUT 401` 用尽 `MAX_DELIVERY_SENDS`，停在 `registered`；attempt 窗口到期删除 Pod 后，`roll_runtime_attempt` 以 `attempt_has_unsettled_run` 拒绝滚动。根因链：部署级 24h bearer 与 attempt 寿命不匹配 → supervisor 转发授权时被平台按 `exp` 拒绝 → 401 折叠为 `STALE_EXECUTION` → 权威拒绝消耗投递预算 → 无"从未被观测 Run"的结算路径。**未修复**：诊断只写证据与队列，不改代码、不执行 roll；后续修复项见[2026-09-16 任务队列](next-development-plan-2026-09-16.md)，证据见[证据包](../../vnext/evidence/P11/attempt-credential-expiry-20260916/README.md)。

- attempt 凭据到期续集：孤儿 Run 结算与 attempt 3 真实派发（2026-09-16，代码 `3307a14`，迁移头 `vnext_0021_p05_environment_settlement`）：迁移 0021 + `ControlService._settle` 让"环境已消失且无任何执行证据"的 Run 有界投影为 `incomplete` 并以 `environment_stopped_before_observation` 收口，runtime 只在 Pod 对象确认不存在时提供该证据。真实 K8s：attempt 2 的 `9e574eb0` 结算后 `roll_runtime_attempt` 放行，刷新 receiver bearer 并滚动到 attempt 3 后，新 Run `993bea1b` 被真实投递（`PUT 200`、22×`GET 200`、0 次 401/URLError）、观测到 `started`/`exited`；公开命令路由的幂等重放返回同一 `request_id`（202）。**未通过**：该 child 以 `native root exceeds the fixed object bound` 退出，未产出 accepted 结果（队列 T6）；T2 仓库内凭据刷新命令仍缺。见[证据包](../../vnext/evidence/P11/attempt3-recovery-20260916/README.md)。

- 会话边界超限修复（2026-09-16，代码 `7ff1281`，含 `1ffb8a3`）：`published_session_profiles` 按准入限额派生 Session 界限并以新身份 `harness.<kind>.deployment.v2` 发布（旧 Task 继续钉 v1，runtime host 与共享 ConfigMap 都拒绝同 ref 不同字节）；`NativeSessionAdapter._bounded` 点名超限 root 与字节数。真实 K8s：公开入口新建的 Task `083f6134-…` 首次 attempt 的 `reason` run `c3e30b1f` 两次模型调用后 `exit_code=0`、无私有失败文件、`result_receipt=accepted`、work `done`；窗口内 `PUT 200×2`、`GET 200×26`、0 次 401/URLError。**未覆盖**：同 Task 的 explore run 仍因工作区互斥 `LIMIT_BLOCKED/429` 与一次模型门连接错误收口 `failed/process_failure`（队列 T7）；长会话压缩/记忆与凭据刷新入库仍待办。见[证据包](../../vnext/evidence/P11/history-bound-fix-20260916/README.md)。

- 并发只读与每 Run 工具限额（2026-09-16，代码 `427882b`）：P06 的 `max_inflight_tools` 改为每 Run 计数；声明 `allowed_target_kinds=["workspace_read"]` 的工具按 Run 共享路径（claim 键 `…:read:<agent_run_id>`，只与裸路径独占者冲突），写者保留裸键并通过前缀检查排除读者。真实 K8s：公开入口新建 Task `fc2ff1b0-…` 首次 attempt 中 reason 与 explore 两个 Run 于 12:39:27/28 同时启动并读取同一 `version.txt`，两者 `exit_code=0`、无私有失败文件、`result_state=accepted`、work item 均 `done`；两条 per-Run claim 可见于证据包。测试：`test_run_admission`+`test_remote_workspace`+`test_capture_transactions` 79 passed、`test_maf_child_transport` 22 passed。**未覆盖**：写者路径无真实夹具；多 Task 并发、长会话压缩/记忆与 T2 凭据刷新入库仍未做。见[证据包](../../vnext/evidence/P11/concurrent-read-fix-20260916/README.md)。

- 凭据刷新入库与守卫（2026-09-16，代码本项提交）：新增 `scripts/vnext/refresh_credentials.py`，固定 `runtime-credentials/scheduler-credentials/gates-credentials/api-credentials` 六个字段的计划，TTL 限制 1..72 小时（默认 24），当 `tasks_in_window>0` 或 `enabled_receivers>0` 时拒绝刷新（陈旧未退出 Run 不阻断），输出只含有界元数据，支持 `--dry-run` 与 `--restart`。真实集群：dry-run 守卫计数 `{0,0,6}` → 实际刷新 patch 四个 Secret 并滚动 api/runtime/scheduler/gates（四个 rollout 成功）→ 新 receiver bearer `iat 2026-09-16T14:29:43Z / exp 2026-09-17T14:29:43Z`，T1 启动守卫 `remaining=86342s` 通过。测试：`tests/vnext/test_credential_refresh.py` 15 passed。**未覆盖**：拒绝分支仅有单元覆盖；bearer 仍是部署级共享，按 attempt 独立签发未设计。见[证据包](../../vnext/evidence/P11/credential-refresh-20260916/README.md)。

- P12 完成 precheck 生产者（2026-09-16，代码 `3d2b55c`，迁移头 `vnext_0022_p12_completion`）：新增 SECURITY DEFINER 的 `vnext.prepare_completion_quiesce`（控制权限 + 已激活 + 未进入完成期 + control_version/board_revision 匹配 + receipt key 幂等），`completion.criteria` 只接受已持久化的 `criterion_judgment`（缺失/unknown/not_applicable/非 current 均未满足，空 required 永不满足），`CompletionService.precheck` 不绑定提出者 Run，`propose` 仅在 ready 时写决定、`apply` 交由既有 P05 状态机。真实 K8s：迁移应用后对运行时服务的 7 个 Task 跑 precheck，全部 `wait/criteria_unmet`（含两个 Run 都已接纳、work 均 `done` 的 `fc2ff1b0-…`）。测试：`test_completion_protocol.py` 8 passed；相邻 5 个套件合计 49 passed。**未覆盖**：判定生产、quiescing 期间结算/拒绝新动作、close_trigger 与 result_outcome 分离、ReportCommit 冻结与迟到反证（AC-049/052/053）仍待实现。见[证据包](../../vnext/evidence/P12/completion-precheck-20260916/README.md)。

- P12-B 判定生产与完成期（2026-09-16/17，代码 `267f720` + `91a22b5`，迁移头 `vnext_0023_p12_judgments`）：`vnext.record_criterion_judgment`（冻结 revision/定义、按 judgment key 幂等、仅当 revision 仍最新才移动 `current_judgment_id`，迟到反证记录但不改当前）与 `vnext.prepare_completion_close`（`close_trigger` 与 `result_outcome` 独立、必须匹配当前 epoch/control_version/board_revision）；`completion.judgments` 仅 assessor+`can_assess` 可写、只接受已封存且可读的证据并记录实际校验 digest；`CompletionService.close` 写关闭决定。真实 K8s：Task `fc2ff1b0-…` 用真实封存 artifact（sha256 465ee220…）写入 `met/current` 判定 → precheck `ready` → 写 quiescing 决定 → apply 后 `observed_state=quiescing`、`execution_allowed=false`、epoch `da99b237-…`；同 key 不同回执重放被拒。测试：`test_completion_protocol.py` 12 passed、相邻 2 套件 62 passed。**未覆盖**：判定的自动生产来源、真正 close（需全部 Run 已结算）、AC-049 冻结期结算、AC-053 ReportCommit 冻结与迟到反证追加。见[证据包](../../vnext/evidence/P12/completion-judgment-20260916/README.md)。

- P12-C 冻结期结算与强制关闭（2026-09-17，代码 `3c0b3b4`）：测试证明 `quiescing` 期间新动作被拒（`task_can_run` 假、不可派发）而在飞 Run 的 `exited` 观测、`result_submission` 与 accepted 投影仍被接受（AC-049）；`CompletionService.close` 仅对 `goal_satisfied` 要求完整评审，强制关闭（budget/time/no_progress/operator）允许未完成工作并由 P05 取消，`close_trigger` 与 `result_outcome` 分离（AC-052）。真实 K8s：Task `fc2ff1b0-…` 由 `quiescing` 走到 `observed_state=closed`（`goal_satisfied`/`complete`/`execution_allowed=false`/control_version 4），两份 completion_decision（quiesce、close）与判定、已结算 Run 事实全部落库。测试：`test_completion_protocol.py` 14 passed、相邻 4 套件 41 passed。**未覆盖**：AC-053 ReportCommit 冻结与迟到反证追加、关闭的产品入口与报告投递、判定的自动生产来源。见[证据包](../../vnext/evidence/P12/completion-close-20260917/README.md)。

- P12-D 报告冻结与迟到反证（2026-09-17，代码 `9bddbcc`，迁移头 `vnext_0024_p12_reports`）：`vnext.freeze_report_commit` 只允许在该 epoch 上已关闭的 Task 冻结报告，digest 由服务端计算，同 key 不同字节或落库正文与自身 digest 不一致都拒绝；`vnext.amend_report_commit` 追加争议记录并把 commit 标成 `disputed`，永不修改正文；`completion.reports` 由持久事实组装正文并校验反证引用的封存证据与 clearance。真实 K8s：已关闭 Task `fc2ff1b0-…` 的报告 684 字节冻结（digest `4445bc13e4e29cf2…`），追加一条引用真实封存 artifact 的迟到反证后正文与 digest 完全不变、`dispute_state=disputed`、Task 保持 `closed`、`ready_work=0`；同 key 不同字节与被篡改 digest 的行均被拒。测试：`test_completion_protocol.py` 16 passed、相邻 5 套件 68 passed。**未覆盖**：产品入口（工作台/API 触发关闭与查看报告）与 ReportDelivery、受信来源清单、判定自动生产、多 Task 并发。见[证据包](../../vnext/evidence/P12/report-freeze-20260917/README.md)。

- P12-E 关闭与报告的产品入口（2026-09-17，代码 `6148154`）：`GET/POST /api/v2/tasks/{task_id}/completion` 与 `GET /api/v2/tasks/{task_id}/reports/{report_id}` 组合既有平台决定（`GET` 返回审核 + 报告摘要；`POST` 的 `quiesce` 打开 epoch、`close` 消费一条平台决定并冻结报告），决策字节仍由 `prepare_completion_*` 写、P05 消费，审核与结算状态全部从数据库读；持有该 Task `can_control` 的 `operator` 可操作、agent 排除，同源 BFF 只转发固定路由。真实 K8s + 浏览器（44180）：Task `083f6134-…` 由面板 `ready` → `quiescing` → `closed`，报告 701 字节冻结（digest `4f1b3e37…`、`clear`、level=1），新 Idempotency-Key 重放返回同一份 commit，缺号报告 404，`ready_work=0`；控制台只有登录前预期 401。检查：`test_completion_portal.py` 6 passed（真实 PostgreSQL + 签名 HTTP）、`test_completion_protocol.py` 16 passed、web build 与 contracts check exit 0（22 passed 合计见证据包）。**未覆盖**：强制关闭（`operator_finish` 等）仅由真实 PostgreSQL 用例覆盖、报告投递与历史列表未实现、判定仍由平台侧断言主体写入、多 Task 并发仍待做。见[证据包](../../vnext/evidence/P12/product-entry-20260917/README.md)。


- P15 受权 ViewStream 与工作台实时视图（2026-09-17，代码 `32311a4` + `94a1122`，迁移头 `vnext_0025_p15_view_stream`）：`vnext_0025_p15_view_stream` 把 P13 冻结的 `view_revision=1` 放宽为可推进，并新增受 subject/`wuji.snapshot`/materialization 约束的 UPDATE 策略、`stream` 游标类型与 `vnext.task_for_view`；`ProjectionRepository.stream_step` 为同一视图写出新 materialization、生成不超过 2000 条的 node/edge patch 与续传游标（无可见变化不发批次），`GET /api/v2/views/{view_id}/events` 输出 SSE 批次，权限撤销/视图或快照过期/变更过大时输出 `ViewReset(action=resnapshot)`；同源 BFF 只转发该 Task 拓扑刚发布的 view id，工作台对 live 视图应用 patch，失败或 reset 时显式重读快照并显示连接状态。真实 K8s + 浏览器：`curl -N` 收到 keepalive 与 16 条 patch 的批次（真实 `pause` 命令 202 触发），浏览器不刷新页面即把画布节点从 `origin:2abfdd57…@4` 更新为 `@5`；数据库：view_revision=3、stream 游标 3 条、三条 projection 策略、迁移头 0025。检查：pytest 29 passed（真实 PostgreSQL + 签名 HTTP）、topology vitest 29 passed、web build exit 0。**未覆盖**：跨 BFF 重启的视图账本丢失、多标签长连接公平性、规模 p95，以及 `ViewReset` 的过期/过大原因只有单元覆盖。见[证据包](../../vnext/evidence/P15/view-stream-20260917/README.md)。

- T8 每 Task 独立 supervisor 端点（2026-09-17，代码 `425a187`）：launcher 为每个 attempt 创建 `task-agent-<task 前 12 位>`/`task-kali-<task 前 12 位>` Service，并把 `service_names` 与 `supervisor_url` 写进该 Task 的 runtime 条目；gates executor 按 Task 指向自己的 Kali；runtime 新增 `TaskSupervisorTransport` 按 Task 路由 `query/start/control`（未声明端点的旧条目仍回落部署 URL），`PodEnvironment` 按 Task 校验 Service 端点 UID；两个 task Service 叶证书新增一层通配 SAN，`k8s.py rotate-task-certs` 复用既有 CA 只重签这两张叶证书。真实 K8s：Task `abbeab81-…` 与 `989eead2-…` 的 Pod 同时 Running，两个 per-Task Service 的 EndpointSlice 分别只指向自己的 Pod，两侧各 2 个 Run 全部 `exited|accepted|settled`，runtime 两条 `PUT 200` start，且每个 Pod 容器内的 supervisor 收件箱只持有自己 Task 的两个 assignment（task/run/receiver 身份全匹配）。检查：`test_task_supervisor_routing.py`/`test_pod_task_config.py`/`test_task_certificates.py`/`test_k8s_runtime.py`/`test_runtime_batch_isolation.py`/`test_task_launch.py` 53 passed。**未覆盖**：旧条目仍回落固定名（与新 Task 同时运行会抢名）、多 Task 长时间公平性、per-attempt 证书签发、写入路径实机与长会话压缩。见[证据包](../../vnext/evidence/P11/multi-task-endpoints-20260917/README.md)。

- P16-A 报告交付与交付媒介适用性（2026-09-17，代码 `6cfa411` + 修复 `3843311`，迁移头 `vnext_0026_p16_report_delivery`）：新增只读的 `vnext.report_delivery` 与 `SECURITY DEFINER` 生产者 `vnext.record_report_delivery`（要求 `wuji.control`、当前 scope、已关闭 Task 的同一 epoch，且必须引用冻结正文 digest；服务端计算 profile/manifest 摘要）。`wuji_core.audit.delivery.ReportDeliveryService` 按显式声明的 profile 索引冻结正文与**已密封**产物，`ready`/`incomplete` 由缺失的必需材料决定，可选要求不阻塞；数据库层守卫拒绝“缺必需材料却写 ready”、离线交付携带 HTTP 交换、http ready 缺少真实交换回执、同一 key 改写历史。产品面新增 `GET/POST /api/v2/tasks/{id}/reports/{rid}/deliveries` 与单条/列表读取、同源 BFF 白名单（仅本 Task 该路径）、工作台“报告交付”面板。真实 K8s：离线文档 profile 记录 `ready`（`manifest_digest=2a7922b9…`、`exchange=null`），显式要求 `image/*` 的 profile 记录 `incomplete`（缺 1 份必需材料），同 key 重放返回同一记录，离线带交换 → 422、缺 `Idempotency-Key` → 422、`reader` → 404、其他 Task → 404；数据库中 3 条记录状态自洽且无 `ready` 缺清单。真实浏览器：面板列出 3 条交付并从 UI 生成新记录（`GET/POST …/deliveries` 均 200，控制台仅登录前 401）。检查：`test_retention_and_delivery.py` 9 passed、`test_completion_portal.py`/`test_completion_protocol.py` 全绿（新增“未开 epoch 直接关闭 → 409 `COMPLETION_EPOCH_ABSENT`”断言）、`test_web_gateway.py` 6 passed、合同 check 与 web typecheck/build exit 0。**采集时发现并修复**：公开 `ErrorResponse.code` 是封闭枚举，服务内部码会在边界退化为 500（同一缺陷此前也潜伏在 P12 完成入口的拒绝路径）；`3843311` 把 7 个内部码映射为契约内公开码。**未覆盖**：本集群镜像仍是 `6cfa411`（采集时段 PyPI 与本地代理均不可达，镜像无法重建），该修复只有本机 ASGI 用例证据；保留/GC/purge、审计分离、外部媒介投递均未做。见[证据包](../../vnext/evidence/P16/report-delivery-20260917/README.md)。

- 阶段统一回归（2026-09-17，代码 `db4954a`）：`pytest tests/vnext` **535 passed / 0 failed / exit 0**（861.66s，本地 fixture PostgreSQL、每用例独立库与真实应用角色/迁移）；同轮 `contracts:check:v2`、Node Supervisor（22 passed）、拓扑 Vitest（29 passed）、web typecheck+build 全部 exit 0。第一次全量运行为 532 passed / 3 failed，暴露并修复两处真实问题：T8 的 `task_id` 关键字打漏了 `test_maf_child_transport` 的测试替身签名；0016 升级检查未跳过新 head `upgrade_view_stream`。同时补齐 CI 阻塞：`packages/maf-worker` dev 组加入 `kubernetes==36.0.3`（否则 `test_pod_runtime.py`/`test_configure_refresh.py` 在 `pnpm test:vnext` 下无法收集）、`test_configure_refresh.py` 对齐 `pod_runtime.tasks[]` 形态、CI 拓扑 Vitest 改为 `--config tests/topology/vitest.config.ts`。**这不是产品验收结论**：真实 K8s 故障矩阵、真实模型计费与 P16–P20 仍未覆盖。见[回归证据包](../../vnext/evidence/stage-regression-20260917/README.md)。

运行中仅用自建夹具；HTTP/UI成果提供完整交互及截图，离线记录按其原生媒介保留。自行检查与代理审查如实区分；不宣称第三方认证。P13 纯片由主代理委派的 SOL/xhigh 执行，不冒称主代理独立测试。记录文档的提交与被测代码提交分开。

本次 P07/P14 状态更新仅修正实施状态文档的落点，复用上述已绑定代码、测试、截图与审查证据，不重跑验证。导入源包 `docs/vnext/ACCEPTANCE.md` 保持原始字节。

P01 永久证据由 Wuji vNext P01 文档维护，位于 [docs/vnext/evidence/P01](../../vnext/evidence/P01/relocation.json)，不放入可清理的 SDD scratch。当前 [CapabilityRecord](../../vnext/capability-record.json) 与 [实际截图来源](../../vnext/evidence/P01/screenshots/provenance.json) 已发布；原缺图/初审状态与旧运行结果按原样归档，不追写历史通过结论。
