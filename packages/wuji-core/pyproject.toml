[project]
name = "wuji-core"
version = "0.1.0"
requires-python = "==3.13.15"
dependencies = [
  "cryptography==50.0.1",
  "fastapi==0.141.1",
  "joserfc==1.7.5",
  "pydantic==2.13.5",
  "simplejson==4.1.2",
]

[build-system]
requires = ["hatchling==1.29.0"]
build-backend = "hatchling.build"

# An independent project; never resolve the legacy root workspace.
[tool.uv.workspace]
members = []
