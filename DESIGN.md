# Wuji 工作台视觉基线

本文件记录 2026-09-09 工作台布局和后续多配色实现。用户已确认三栏风格，并明确要求正式工作台内置五套可切换主题；这是正式前端的实施基线。当前运行代码仍属于本地原型，正式品牌标识未冻结。

## 结构

桌面采用 64 px 导航轨道、58 px 项目栏和连续三栏工作区：左侧任务队列与范围、中间任务执行、右侧证据检查器。列表入口与任务深链接共用工作台；创建任务保留独立表单，完整证据使用正文与属性双栏。

1250 px 以下，证据检查器移至执行区下方；900 px 以下纵向排列；600 px 以下导航轨道缩至 48 px。长响应在正文容器内滚动，禁止撑宽整个页面。

## 色彩与组件

正式工作台内置以下五套主题，默认使用雾银。新增页面、抽屉、表单、证据查看器和后续图表都使用相同语义色表，随当前主题切换。

| 配色 | URL 参数 | 内容与强调 | 使用算法 |
| --- | --- | --- | --- |
| 雾银（默认） | `theme=silver` | 浅银灰外框、近白内容面、深青蓝强调 | defaultAlgorithm |
| 冰蓝 | `theme=glacier` | 冰蓝外框、近白内容面、深蓝强调 | defaultAlgorithm |
| 青瓷 | `theme=celadon` | 浅青外框、近白内容面、深青绿强调 | defaultAlgorithm |
| 亮石墨 | `theme=slate` | 提亮的蓝灰内容面、高亮文字、浅青强调 | darkAlgorithm |
| 原石墨 | `theme=graphite` | 保留原深石墨背景作对照 | darkAlgorithm |

颜色集中定义在 `spikes/frontend/src/shared/theme.ts`；正式工程初始化时迁入 `apps/web/src/app/` 的主题模块。CSS Modules 和 Ant Design 从同一语义色表取值。每套覆盖背景、内容面、浮层、边界、正文、辅助文字、占位符、选择状态、成功/警告/错误和证据正文。

- 当前主题的强调色表示选择和主要操作；绿色表示已确认结果；琥珀色表示待核对、取消中与清理等待；红色用于错误和取消操作。
- 单个 Ant Design ConfigProvider 根据主题切换 defaultAlgorithm / darkAlgorithm；全局及组件 tokens 控制颜色、34 px 控件高度、13 px 基础字号、5 px 圆角。浅色主题主按钮使用白字，深色主题主按钮使用深色文字；分别检查对比度。
- CSS Modules 负责领域布局；不覆盖 Ant Design 内部类名。Tabs、Segmented、Input、Select、Button、Drawer、Tooltip 使用公开 API。
- 不使用装饰渐变、光晕、玻璃卡片、无意义大数字或虚构运行指标。

页头配色选择器仅切换外观，不重建任务或清空表单。有效 URL 参数优先，其次读取标签页 sessionStorage，再回退到雾银；存储不可用时仍可切换。不同标签页各自保持选择，便于浏览器比较。选中的主题随路由跳转保留，页面刷新后恢复；浏览器标题包含主题名称。

上述 sessionStorage 和 URL 优先级属于原型比较模式。正式工作台按设备持久保存非敏感主题 ID，重新打开后恢复；存储不可用时回退到雾银。显式比较链接仍允许临时覆盖当前标签页，不覆盖设备偏好。切换主题不触发业务命令、重新执行、会话变更或表单重置。账户级跨设备同步按后续需求增加。

## 字体

界面拉丁字形使用本地打包的 Inter Variable，中文使用 PingFang SC / Microsoft YaHei / 系统字体。地址、编号、时间、响应正文使用 IBM Plex Mono。两个字体包固定为 5.3.0，登记许可为 OFL-1.1；字体不依赖外部 CDN。

主标题 21 px，任务标题 19 px，正文与控件 12–13 px，技术辅助字段 10–12 px；目标地址、证据正文与日志辅助文字较第二版分别提高约 1 px。窄窗口按结构重排，技术字段适度收紧。

## 文案与行为

页面保留名称、目标、范围、状态、执行记录、证据以及可执行动作。删除功能口号和实现解释，例如“跟踪授权验证的执行、证据和资源回收。”。本地演示标识留在项目栏；内存数据说明、异常场景和模拟回执放进设置抽屉。

取消动作只表示取消请求已接受；独立执行回执后才显示已停止。结果不明、执行终态和资源清理继续分开展示。证据正文始终作为文本输出；代码行号不进入辅助技术朗读。

保留键盘跳转入口、可见焦点、字段错误关联和浮层焦点恢复。状态不只依赖颜色；系统减少动态效果时禁用过渡。完整可访问性和真实服务集成仍需单独验收。
